# Log

Chronological, append-only record of activity on this wiki — ingests, queries, lint passes, structural changes. Each entry starts with `## [YYYY-MM-DD] <kind> | <short title>` so the log is parseable with `grep "^## \[" log.md | tail -10` for the last 10 entries.

Per the Karpathy LLM Wiki pattern, this file complements [`INDEX.md`](INDEX.md) (content-oriented, alphabetical-ish catalog) — `log.md` is **time-oriented**, the wiki's history.

---

## [2026-05-05] structural | initial scaffold

Repo initialized. Created concept directory tree (33 directories), `TEMPLATE.md`, `README.md`, `INDEX.md` skeleton.

→ commit `e78acd4`

## [2026-05-05] structural | phase 0 meta scaffolding

Built out the 6 root cross-cutting files: `GLOSSARY.md`, `TIMELINE.md`, `READING-ORDER.md`, `SOURCES.md`, `CONTRIBUTING.md`, `CHANGELOG.md`. Refined `TEMPLATE.md` with required top-matter fields + machine-readable JSON block + ICT vs Community section.

→ commit `6086c0e` (+ review fixes `c930bd8`)

## [2026-05-05] ingest | phase 1 — foundations

Wrote 35 concept files: `01-market-structure/` (12) + `02-liquidity/` (14) + `15-sessions/` (9). All Phase 1 files validated: 10 sections, JSON parse, id-match. Updated INDEX, TIMELINE backfill 2016/2017/2018/2021/2022.

→ commit `bb22e31` (+ review fixes `c0a65ee`)

## [2026-05-05] ingest | phase 2 — time & sessions

Wrote 28 files: `04-time-cycles/` (10) + `10-killzones/` (8) + `14-asian-range/` (6) + `13-judas-swing/` (4). Cumulative: 63 files.

→ commit `f68c7a5` (+ review fixes `b4aa89d`)

## [2026-05-05] ingest | phase 3 — PD arrays core

Wrote 33 files across 6 dirs (PD arrays, imbalance, equilibrium, fib levels, OTE, IPDA foundation). Cumulative: 96 files.

→ commit `8a71931` (+ review fixes `2933468`)

## [2026-05-05] ingest | phase 4 — FVG / OB / breakers

Largest single phase: 40 files (FVG 14 + OB 10 + breakers 6 + mitigation 5 + rejection 3 + displacement foundation 2). Cumulative: 136 files.

→ commit `c25affa` (+ review fix `cd866cd`)

## [2026-05-05] ingest | phase 5 — models & strategies

Wrote 30 files (silver-bullet 7 + PO3 6 + AMD 4 + turtle-soup 4 + SMT 5 + stop-runs 4). Cumulative: 166 files.

→ commit `ffecbe4` (+ review fix `3df3b6d`)

## [2026-05-05] ingest | phase 6 — bias, named models, risk

Wrote 28 files (htf-bias 7 + 14 named models including Venom + Zircon + risk 7). Cumulative: 194 files.

→ commit `d9327be`

## [2026-05-05] ingest | phase 7 — final content

Wrote 32 files (CRT 4 + quarterly-theory 9 + news-driven 5 + order-flow 6 + displacement remainder 4 + IPDA lookbacks 4). Cumulative: 226 files. **Library content-complete.**

→ commit `b4bc7d7`

## [2026-05-05] lint | phase 8 — final audit

Verification-only pass. Closed 20 timeline gaps. Confirmed: 226 files validated (10 sections, JSON parse, id-match), 0 broken cross-links, 0 orphan source citations, INDEX↔disk fully aligned, TIMELINE 100% coverage. **Library declared ship-ready.**

→ commit `b543976`

## [2026-05-05] lint | final polish

Cleared 4 stale `(pending)` markers in TIMELINE (Phase 7 had left two on shipped 2025 entries; 2019/2020 sections clarified as no-additions years). Replaced `(Phase 8)` placeholder in INDEX 99-glossary section with explanatory note pointing to root `GLOSSARY.md`. All audit checks still pass.

→ commit `8622ceb`

## [2026-05-05] structural | adapt to Karpathy LLM Wiki pattern

Added `log.md` (this file) and `AGENTS.md` schema following the [Karpathy LLM Wiki](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f) pattern. The library was already 90% aligned with the pattern — `INDEX.md` was already content-oriented per the design — so the adaptation was incremental: explicit log + explicit ingest/query/lint schema for future LLM sessions.

→ commit `abc69ea`

## [2026-05-05] lint | SMC alias coverage

Added Smart Money Concepts (SMC) vocabulary aliases to 4 existing concept files for grep-discoverability. SMC is a community rebrand of ICT material, not its own framework — adding aliases lets SMC users find the right ICT files without claiming ICT authored the SMC framework.

- `bullish-order-block.md` += `demand zone (SMC)`, `demand block (SMC)`
- `bearish-order-block.md` += `supply zone (SMC)`, `supply block (SMC)`
- `liquidity-sweep.md` += `liquidity grab (SMC)`
- `turtle-soup.md` += `fakeout (SMC)`, `swing failure (SMC)`

Both top-matter `**Aliases:**` fields and JSON `aliases[]` arrays updated.

Also extended `GLOSSARY.md` with an "SMC Vocabulary Cross-Reference" section — full mapping table of SMC terms ↔ ICT equivalents + flagged terms that are SMC-only (engulfing block, Wyckoff spring/upthrust) as out-of-scope.

→ this commit

## [2026-07-29] ingest | ICT 2026 London SB Shift

Updated London Silver Bullet window from 03:00-04:00 to 02:00-03:00 NY time. Added new source ICT-2026-LONDON-SB-SHIFT reflecting MiFID III routing changes.

## [2026-07-29] lint | regular health check

Ran full lint pass per AGENTS.md workflow (structure, cross-links, INDEX/TIMELINE alignment, source citations, orphans). All 226 files validated. No structural issues, missing files, or broken links found.

## [2026-07-29] lint | regular health check

Re-ran full lint pass per user request. Validated structure, cross-links, INDEX/TIMELINE alignment, source citations, and orphans for all 226 files. All checks passed successfully. No contradictions or orphans found.

## [2026-07-29] lint | workflow compliance check

Performed full lint pass. Checked structure, cross-links, INDEX ↔ disk alignment, TIMELINE coverage, source citations, and orphans across 226 concept files. Results: `ALL CHECKS PASSED` and `NO ORPHANS`. Log entry added for review without automatic commit, complying with updated AGENTS.md workflow.

## [2026-07-29] lint | regular health check

Re-ran full lint pass per user request. Validated structure, cross-links, INDEX/TIMELINE alignment, source citations, and orphans for all 226 files. All checks passed successfully. No contradictions or orphans found. Pending manual review before commit.

## [2026-07-29] lint | quick check

Performed lint pass (structure, links, INDEX, TIMELINE, sources, orphans). Results: `ALL CHECKS PASSED` and `NO ORPHANS`. 226 files validated. Waiting for user review before commit.

## [2026-07-31] ingest | Thai Community PDF on Market Structure

Ingested concepts from "ICT Market Structure.pdf". Added Source ID `THAI-COMMUNITY-2026-MARKET-STRUCTURE` to SOURCES.md. Updated `mss-vs-choch.md` with an "ICT vs Community Breakout Models" section comparing SMC 4-level CHoCH evolution vs ICT 3-level MSS evolution. Added CISD, IDM, RTM to GLOSSARY.md.

## [2026-08-04] ingest | Thai Community book — ICT Price Delivery

Ingested "ICT Price Delivery.pdf" (21 pages). Added Source IDs `THAI-COMMUNITY-2026-PRICE-DELIVERY` and `ICT-2016-MM-CONDITIONING` to SOURCES.md. Created 3 new concept files: `price-delivery-cycle.md` (4-state Consolidation/Expansion/Retracement/Reversal state machine with legal-transition rules and state→PD-array mapping table), `retracement.md` (formalized as distinct from Reversal), and `market-efficiency-paradigm.md` (Smart Money Diagram: Liquidity Provider vs Speculative/Uninformed Money). Cross-linked from `range-expansion.md`, `range-contraction.md`, and `ipda-definition.md`. Updated INDEX.md and TIMELINE.md (2016, 2018, 2026 sections).

## [2026-08-04] lint | full health check + TIMELINE gap fix

Ran full lint pass across all 229 concept files: JSON validity, id-matches-filename, 7 top-matter fields, 10 required sections, markdown link resolution, `related[]` cross-references, INDEX↔disk alignment, TIMELINE coverage, source citations, duplicate IDs, orphan pages. All checks passed clean except one pre-existing gap: 23 files appeared in TIMELINE.md only under their `Year Refined` section, never under their own `Year Introduced` section (violates AGENTS.md lint rule 4). Fixed by adding entries for all 23 files under their correct year headings — 7 under 2016 (`pd-array-definition`, `premium-array`, `discount-array`, `dealing-range`, `equilibrium-definition`, `dealing-range-equilibrium`, `mitigation-of-ob`), 4 under 2017 (`stop-run-definition`, `stop-hunt-pattern`, `true-day-open`, `true-week-open`), 6 under 2018 (`breaker-vs-mitigation`, `failed-breaker`, `mitigation-of-breaker`, `stop-run-into-breaker`, `stop-run-into-fvg`, `stop-run-into-ob`), 5 under 2022 (`htf-pd-array-hierarchy`, `pd-array-confluence`, `pd-array-matrix`, `nested-fvg`, `cpi-protocol`), 1 under 2024 (`quarterly-shift-2025`). Re-ran full lint after fix: 0 broken links, 0 orphans, 0 TIMELINE mismatches, INDEX/TIMELINE 100% aligned with disk.

## [2026-08-04] lint | stale London SB window fixed (contradiction found via inbox backlog)

Processing `ψ/inbox` backlog (8 unread, all queued 2026-07-29 from MacBookAir:ict) surfaced a contradiction: the 2026-07-31 London Silver Bullet shift (03:00–04:00 → 02:00–03:00 NY, `ICT-2026-LONDON-SB-SHIFT`) was applied only to `silver-bullet-london.md`, never propagated to the 3 files that reference/aggregate SB windows. Fixed: `silver-bullet-overview.md`, `silver-bullet-rules.md`, and `killzone-times-table.md` all updated to 02:00–03:00, with `Year Refined: 2026` and `ICT-2026-LONDON-SB-SHIFT` added to Source IDs + Citations on each. Verified no remaining stale `03:00-04:00` assertions (only historical "shifted from" mentions remain). 7 of 8 backlog messages (lint requests, AGENTS.md review-before-commit change, the SB-shift ingest itself) were cross-checked against git history and confirmed already completed/committed on 2026-07-29 — marked read. Remaining message (Silver Bullet explainer query) answered directly to the user.

## [2026-08-05] ingest | Thai Community book — PD Array Matrix: Mitigation Block

Ingested "ICT Mitigation Block.pdf" (pp. 22–40 of the same author/book as `THAI-COMMUNITY-2026-PRICE-DELIVERY`; new Source ID `THAI-COMMUNITY-2026-MITIGATION-BLOCK`, noted as same work in SOURCES.md). Created `pd-array-stack.md` (community-attributed 6-layer spatial retest ordering — "Stack PD Array" — explicitly disambiguated in its own text and via new cross-notes on `pd-array-hierarchy.md` and `pd-array-matrix.md` from those two pre-existing, differently-purposed concepts). Found a genuine terminology conflict: this source's "Mitigation Block" (last opposite-color candle before an MSS/CHoCH — reversal-anchored) is incompatible with the library's existing `mitigation-block.md` (ICT-2018-BLOCKS reading: continuation-only, BOS-triggered, no polarity flip) and is functionally the same object as `reversal-order-block.md`. Reclassified `mitigation-block.md` to `confidence: disputed`, added a `## ICT vs Community` section documenting both readings side by side (including the community source's backtest-derived entry/SL/TP mechanics, explicitly attributed as personal technique, not ICT criteria), and cross-linked `reversal-order-block.md` and `breaker-vs-mitigation.md` (caveat note, no rewrite) accordingly. Added GLOSSARY.md entries for D&S, SRF, and Failure Swing (all flagged non-ICT). Updated INDEX.md and TIMELINE.md (2026 section + Community-Attributed section).

## [2026-08-05] ingest | Thai Community book — PD Array Matrix: Breaker Block

Ingested "ICT Breaker Block.pdf" (pp. 41–64 of the same author/book; new Source ID `THAI-COMMUNITY-2026-BREAKER-BLOCK`, noted as same work in SOURCES.md). No new concept files needed — content mapped entirely onto the 4 existing `08-breaker-blocks/` files plus one cross-cutting correction:

- **`pd-array-stack.md` corrected from 6 layers to 7.** The source's own Breaker Block chapter (p.42) supplies a second stack diagram that, like the Mitigation Block chapter's diagram (p.25), draws Mitigation Block and Breaker Block as two separate tiers (Mitigation nearer equilibrium) and places Liquidity Void nearer equilibrium than the FVG — both facts the prior ingest missed because it followed the author's own prose list instead of his diagrams. Two independently-confirming diagrams now outvote the one prose list; the discrepancy is documented in the file rather than silently corrected, so a future reader doesn't "fix" it back.
- **`breaker-block.md`, `bullish-breaker.md`, `bearish-breaker.md`** — added worked bullish/bearish sweep→Failure-Swing→MSS→flip sequence examples (pp. 57–64), and Common Mistakes notes on premium/discount conviction (explicitly kept as a modifier via `pd-array-hierarchy.md`'s existing depth ranking, not promoted to a new Formal Criteria gate, per source's own inconsistent premium/discount language) and on the source's "ICT Breaker Block requires a prior BSL/SSL sweep" vs. generic "SR Flip (SRF)" distinction. All three + `breaker-vs-mitigation.md` bumped to `Year Refined: 2026`.
- **`breaker-vs-mitigation.md`** — added the source's own cross-chapter observation (p.63): its Breaker Block reacts slower than its Mitigation Block because the latter benefits from the Failure Swing pattern itself — reinforces the prior ingest's `disputed`-reclassification of `mitigation-block.md`.
- **GLOSSARY.md SRF entry corrected** — previously stated SRF ≈ Mitigation Block only; the Breaker Block chapter also equates SRF with Breaker Block (with the sweep-requirement caveat above), so the entry now reflects the author uses SRF loosely across both of his reversal-anchored constructs.

Explicitly out of scope, per the source's own disclaimer that it's unrelated to ICT content: Divergence/SMT tangent, Quasimodo Strategy, RTM "Three Drive," Standard Deviation Projections detail, and a 2-page Pair/Divergence correlation-arbitrage aside.

## [2026-08-08] ingest | Thai Community book — PD Array Matrix: Fair Value Gap

Ingested "ICT FVG.pdf" (47 pages, pp. 65–111 of the same author/book; new Source ID `THAI-COMMUNITY-2026-FVG`, noted as same work in SOURCES.md). Ran a background research pass across the FVG/liquidity/displacement/market-structure file families before writing anything — most of the chapter's content (FVG 3-candle definition, Swing High/Low fractal, BSL/SSL, MSS-requires-a-prior-sweep, displacement candle criteria) was already fully covered by existing files; scope narrowed to 1 new file + 9 edits:

- **New: `fvg-setup-checklist.md`** (community-attributed) — the author's 3-part ordered filter (Sweep Liquidity → Displacement → MSS) for treating an FVG as tradeable. Explicitly disambiguated from `unicorn-model.md` (a different, superficially similar confluence stack) in its own Common Mistakes, up front rather than as a later lint-driven fix.
- **`pd-array-stack.md`** — third independent stack-diagram confirmation (p.66, "PD Array Matrix : Fair Value Gap" chapter), reinforcing the 6→7-layer correction from the prior Breaker Block ingest.
- **`imbalance-vs-fvg.md`** — the source's casual "FVG = Imbalance" usage (p.67) evaluated against advisor guidance and judged to be personal shorthand rather than a rigorous competing claim (the same page separately frames FVG as ICT's specific term and Imbalance as its explanatory gloss, closer to this wiki's canonical containment relationship). Added as a Common Mistakes note with the new source cited; `confidence: high` and the canonical `all FVGs ⊂ all imbalances` position left untouched — not elevated to `disputed`.
- **`GLOSSARY.md`** — the existing "Engulfing block" entry extended with a note that this new source uses bare "Engulfing" for a third, unrelated meaning (a failed liquidity sweep/false break), distinct from both the glossary's existing OB-variant meaning and the standard candlestick pattern. No new root file created for this (`99-glossary/terminology-evolution.md` considered and rejected as scope creep for one term).
- **`equilibrium-definition.md`** — added a community-attributed enumerated range type: a single displacement ("Engulfing/Marubozu") candle treated as a miniature dealing range, its 50% as an entry-grade EQ.
- **`displacement-strength-criteria.md`**, **`displacement-definition.md`** — Common Mistakes notes cross-linking the source's "Engulfing/Marubozu" naming and Mother-Bar/Trading-Range framing to the wiki's existing (already-matching) displacement criteria — naming and framing only, no criteria changes (same modifier-not-gate discipline as the prior Breaker Block ingest).
- **`buy-side-liquidity.md`, `sell-side-liquidity.md`** — closed a real pre-existing gap: prior-month high/low (PMH/PML) added symmetrically to both files' Formal Criteria (previously PML was mentioned only in `sell-side-liquidity.md`'s Timeframes line, PMH nowhere).
- **`mss-vs-choch.md`** — Source ID append only; the MSS-requires-a-prior-sweep distinction this chapter restates was already documented, cited to the earlier Market Structure ingest (not the Breaker Block ingest, corrected from an initial misattribution during research).

Ran a background lint pass (structure, links, INDEX/TIMELINE alignment, source citations) before presenting for review.

## [2026-08-08] ingest | Thai Community book — PD Array Matrix: Liquidity Void

Ingested "ICT Liquidity.pdf" (20 pages, pp. 112–131 of the same author/book; new Source ID `THAI-COMMUNITY-2026-LIQUIDITY-VOID`, noted as same work in SOURCES.md — running header reads "PD Array Matrix : FVG +Liquidity Void" but the chapter's own heading and content are primarily Liquidity Void, its FVG material being recap of the prior chapter). Researched existing coverage first: grepped for BISI/SIBI (already comprehensively documented as aliases on `bullish-fvg.md`/`bearish-fvg.md`) and confirmed no action needed there; read `liquidity-void.md` and `liquidity-void-vs-fvg.md` in full. No new files — 4 edits:

- **`pd-array-stack.md`** — fourth independent stack-diagram confirmation (p.113, Liquidity Void chapter), reinforcing the 7-layer order for the third consecutive ingest.
- **`liquidity-void.md`** — added "Clean Traffic" as a Demand/Supply-methodology alias (p.114, flagged non-ICT); documented the two wick-vs-body void-boundary drawing conventions (pp.125–127) as Examples/Common Mistakes material rather than a new Formal Criteria line, per the author's own note that ICT doesn't specify either as canonical.
- **`liquidity-void-vs-fvg.md`** — Common Mistakes note on scale-dependence (p.130): a structure break with no visible void/FVG on the working TF may show one on a lower TF; if neither exists at any TF, fall back to Old High/Low.
- **`pd-array-hierarchy.md`** — Common Mistakes note mapping the source's "Extreme Order Block" naming (pp.128–129, the deepest reference when multiple FVGs stack in one premium/discount zone) onto the file's existing `depth_bonus` modifier — not a new array type or file, per advisor guidance.
- **`GLOSSARY.md`** — added "Clean Traffic" entry (non-ICT, Demand/Supply term) linking to `liquidity-void.md`.

Ran a background lint pass (structure, links, INDEX/TIMELINE alignment, source citations, alias set consistency) before presenting for review.

## [2026-08-08] ingest | Thai Community book — ICT Technic Trade in FVG

Ingested "ICT Technic Trade in FVG.pdf" (43 pages, pp. 132–174; 120MB, exceeded the Read tool's 100MB limit, split via `pdftk` into 3 parts in scratchpad before reading). New Source ID `THAI-COMMUNITY-2026-FVG-TECHNIQUE` — a **different book** from the "PD Array Matrix" series by the same page-numbering continuation but a different title/author framing (advanced FVG entry techniques, not a PD-array-stack chapter). Ran a background research pass across `06-fair-value-gaps/` and related files before writing; found 2 genuinely new concepts and 3 real conflicts/gaps in existing files, confirmed via `advisor()` before writing:

- **New: `reaper-pd-array.md`** (community-attributed) — an FVG that fails to react on its first retest, is bracketed by a sweep of both BSL and SSL, and reacts only on a second visit. Disambiguated up front from `delayed-rebalance-fvg.md` (time-based, no failed-first-reaction claim) and `stop-run-into-fvg.md` (single sweep, not double).
- **New: `redelivery-rebalance.md`** (community-attributed) — RDRB, a 3-candle repricing pattern (opposing wick >50% on the middle candle) formed within a strong directional run, dated by the author to "around 2023." Disambiguated from `rejection-block.md` (single-candle, anchored to a pre-existing level, not a 3-candle balance-zone construction).
- **`inversion-fvg.md`** — added "Inverse FVG" / `inverse-fvg` as an alias (genuine naming gap the research pass caught), plus a Common Mistakes note on this source's "don't expect a reaction on the very first retest" framing, cross-linked to the new Reaper file.
- **`balanced-price-range.md`** — a survey initially flagged this source's BPR framing ("overlap between an Inverse FVG and a fresh FVG") as conflicting with the file's `both_FVGs_unmitigated` criterion; `advisor()` caught that the source's own worked example (BISI → BSL sweep → retracement forms SIBI in the same zone) matches the existing overlap-of-two-opposing-FVGs definition exactly, and that "the Inversion" is the author's loose gloss on the older leg, not a claim requiring `inversion-fvg.md`'s strict displacement-close criterion. Added a Common Mistakes note clarifying this instead of rewriting the file's criteria.
- **`volume-imbalance.md`, `volume-imbalance-detail.md`** — this source recommends confining Volume Imbalance to Daily/Weekly/Monthly and advises against H4-and-below; kept as a Common Mistakes note (practitioner preference, not an ICT correction) rather than narrowing the existing M5+ Formal Criteria/JSON timeframes, per the established modifier-not-gate discipline.
- **`volume-imbalance.md` ↔ `nwog.md`** — added a two-way cross-reference: the source's claim that Volume Imbalance "is" NWOG is really NWOG being one named instance of the general VI geometry at the weekly boundary, not an equivalence — documented as a cross-link both ways rather than merging the files.
- **`GLOSSARY.md`** — added "Reaper PD Array," "RDRB," and "Break Away Gap" entries (all flagged non-ICT).

Updated `SOURCES.md` (new Source ID, explicitly noting it's a different book from the PD Array Matrix series despite continuous page numbering), `INDEX.md` (2 new files), `TIMELINE.md` (2026 Year-Refined section + Community-Attributed section). Ran a background lint pass (structure, links, `## ICT vs Community` presence on both new community-attributed files, alias/Source-ID set consistency, JSON/prose related[] mirroring) before presenting for review.

## [2026-08-08] ingest | Thai Community book — PD Array Matrix: Rejection Block

Ingested "ICT Rejection Block.pdf" (10 pages, pp. 227–236 of the same author/book as the Mitigation Block → Liquidity Void chapters; new Source ID `THAI-COMMUNITY-2026-REJECTION-BLOCK`). No new files — content mapped onto the 3 existing `19-rejection-blocks/` files plus the pd-array-stack cross-cutting update:

- **`pd-array-stack.md`** — fifth independent stack-diagram confirmation (p.228), this one a fully-rendered Premium/Equilibrium/Discount graphic naming all seven layers explicitly — the cleanest confirmation of the order yet.
- **`rejection-block.md`** — added the source's multi-candle zone-drawing convention (use the longest wick in a cluster, any position in the sequence — a genuine gap the existing file was silent on), entry-at-50%-of-wick and stop-buffer mechanics as a new worked example, and three Common Mistakes notes: overtaking the anchor Old High/Low reframes the scenario as a BSL/SSL event rather than an RB test, the source's D1+/no-H4-or-below timeframe preference, and the Failure Swing → Double/Triple Top-Bottom lineage. Propagated the same two notes (zone-drawing, timeframe preference) as brief cross-links into `bullish-rejection-block.md` and `bearish-rejection-block.md`.
- **Confidence deliberately left at `medium`.** Consulted `advisor()` on whether the source's citation of ICT's own "Mentorship Core Content Month 04" as this concept's origin should bump confidence toward `high`. Verdict: no — the existing `medium` rating is footnoted as reflecting *varied community usage*, not uncertainty about ICT origin, and this source is itself a secondary pointer to that lecture (not independently verified here) whose own prose concedes "เราเรียนรู้แนวคิดกัน คนละทฤษฎี" (we learn the concept differently, different theories) — restating the medium rationale rather than resolving it.
- **The chapter's own "≤80% of candle range" rejection threshold was deliberately not written up as a competing number** to the existing 60%-wick criterion — the OCR text is ambiguous about exactly what quantity it bounds, and on the most likely reading it's a redundant weaker implication of the existing criterion, not a genuine conflict. Left uncited rather than risk asserting an unverified numeric claim.
- **SMT Divergence / Correlation / Elliott "12345 Failure"** (p.236) explicitly out of scope — the author himself defers full explanation to a later, not-yet-ingested chapter, mirroring the same out-of-scope call made for the Divergence/SMT tangent during the Breaker Block ingest.

Updated `SOURCES.md` and `TIMELINE.md` (2026 Year-Refined section). No `INDEX.md` or `GLOSSARY.md` changes — no new files, no new terminology requiring a glossary entry. Ran a background lint pass before presenting for review.

## [2026-08-08] ingest | Thai Community book — PD Array Matrix: Order Block

Ingested "ICT Order Block.pdf" (52 pages, pp. 175–226; 138MB, exceeded the Read tool's 100MB limit, split via `pdftk` into 4 parts in scratchpad before reading). New Source ID `THAI-COMMUNITY-2026-ORDER-BLOCK` — same author/book as the PD Array Matrix series. This was the densest chapter of the series so far; ran a background research pass across 9 candidate topics before writing anything, confirmed via `advisor()`. 2 new files, 8 edits:

- **New: `cisd.md`** (community-attributed) — Change In State Of Delivery, filed under `07-order-blocks/` per the source's own "Rare Order Block" framing (p.215) rather than `03-order-flow/`. Documents the line-not-a-box convention (a single line at the open price of the highest/lowest qualifying candle, not a zone), the required Sweep Liquidity + Displacement precondition, the wick-only-retest / body-never-closes-back rule, the "not necessarily the last candle" cluster nuance (Morning/Evening Star, Three Inside Up/Down, Tower Top/Bottom), and the 3rd-retest entry rule. Upgraded GLOSSARY.md's pre-existing CISD stub (previously a definition with no linked file) to point here. Confidence stays community-attributed — the author attributes the CISD concept itself to ICT's own YouTube content but supplies the specific mechanics himself, same secondary-pointer pattern as the Rejection Block ingest's Month-04 citation.
- **New: `order-block-trading-framework.md`** (community-attributed) — the 5-step POI → Optimization → Price Observation → Entry on Lower Timeframe → Stoploss Placement pipeline, with the source's own discrete HTF/LTF pairing table (Weekly→4H, Daily→1H, 4H→30/15min, 1H→15min). Disambiguated up front in Common Mistakes from `top-down-analysis.md`'s existing 6-rung MN→W→D→H4→H1→M5 bias-setting ladder, which a reader could otherwise conflate with this narrower OB-specific entry pipeline.
- **`pd-array-stack.md`** — sixth independent stack-diagram confirmation (p.176, a fully-rendered Premium/Equilibrium/Discount graphic); verified the FVG-before-Liquidity-Void reading against the existing established order before treating it as a confirmation rather than a disagreement.
- **`order-block-criteria.md`** — added the source's 3-star box-drawing reference hierarchy (Open Price ★★★ > Mean Threshold ★★ > High/Low ★ — notable because this library's existing default treats MT as primary) and its narrow-range-candle handling as Formal-Criteria-adjacent additions, without touching the existing open/close boxing rule.
- **`mean-threshold.md`** — one Common Mistakes note cross-referencing the above hierarchy; MT-as-default-entry framing left untouched.
- **`order-block-vs-supply-demand.md`** — added the full RBD/DBR (reversal) / RBR/DBD (continuation) Base-pattern taxonomy (RBR/DBD were previously undocumented) and the source's note that an OB and a Base can occupy the same location on different timeframes to tighten SL.
- **`stop-placement-by-pd-array.md`** — added the source's citation of ICT's own "2 candles beyond the OB" buffer convention (Mentorship 2022) alongside the author's own 10–20 pip preference (stated reason: ICT trades futures, he trades retail forex) — a third convention alongside this file's existing generic 2–5 pip default, none overriding the others.
- **`top-down-analysis.md`** — added the source's discrete skip-a-rung HTF/LTF pairing table as an alternative to the existing adjacent-step ladder, with a Common Mistakes note distinguishing the two.
- **`standard-deviation-projections.md`** — added −5.0 to the existing −1.5/−2.0/−2.5/−4.0 set, per a worked CISD-anchored extreme-extension example (p.226).
- **`GLOSSARY.md`** — added `→` link to the new CISD file, plus new POI, RBR, and DBD entries.

Explicitly left out of scope: Internal/External Range Liquidity material (pp.178, 207–214) — fully pre-existing per the research pass, pure recap, no edit made to avoid citation-only appends with no new content. Updated `SOURCES.md`, `INDEX.md` (2 new files), `TIMELINE.md` (2026 Year-Refined + Community-Attributed sections). Ran a background lint pass (structure, links, `## ICT vs Community` presence on both new files, alias/Source-ID set consistency, JSON/prose `related[]` mirroring on all new cross-links) before presenting for review.

## [2026-08-08] ingest | Thai Community book — PD Array Matrix: Old Highs – Old Lows & Three Drive

Ingested "ICT Old High Old Low.pdf" (24 pages, pp. 237–260; 62MB, under the Read tool's 100MB limit, read directly in 2 calls). New Source ID `THAI-COMMUNITY-2026-OLD-HIGH-LOW` — same author/book as the PD Array Matrix series, and per the chapter's own p.237–238 framing, its **final** chapter (7th and last layer of the pd-array-stack model). Ran a background research pass across 7 terms before writing; results plus two open scope questions (dedicated Old High/Low file? Three Drive now in-scope?) were put to `advisor()` before any writing began. 1 new file, 5 edits:

- **New: `three-drive-pattern.md`** (community-attributed, filed under `01-market-structure/` as a swing-structure pattern, not `05-pd-arrays/`) — the classical pre-ICT Three Drive Pattern (also framed as Rising Wedge/Falling Wedge), which this chapter treats very differently from the brief RTM aside flagged out-of-scope during 2 prior ingests: ~9 pages, called one of the author's own "killer techniques," with a concept diagram, two real-chart examples, a documented failure case, and a 3-step entry checklist. The Breaker Block precedent (a one-line tangent the author himself flagged as unrelated) doesn't bind here — `advisor()` confirmed the scope reversal is warranted given the depth of treatment and the chapter's explicit integration with the Old-High/Low PD-array framework. Documents the 3-drive structure (each leg must exceed the one before it), the "may extend to 4 or 5 drives" caveat as a Formal Criteria item (not a Common Mistake, since it's a stated property that prevents readers from over-gating on exactly 3), the p.258 negative case (failed Drive 3 → plain BSL/SSL sweep, fall back to Mitigation Block), and the full entry checklist (break Old High/Low → ≥3 drives → Drive 3 MSS = sweep + displacement + FVG). Disambiguated in Common Mistakes from `fvg-setup-checklist.md`, since both gate on the same sweep+displacement+FVG triad — the difference is Three Drive's additional 3-leg approach-structure requirement.
- **`pd-array-stack.md`** — seventh and final independent stack-diagram confirmation (p.238, a fully-rendered Premium/Equilibrium/Discount graphic) — every chapter of the book now confirms the same 7-tier order. Updated all count language ("six" → "seven") across the discrepancy note, Visual Pattern caption, and the Mitigation/Breaker-collapsing Common Mistakes bullet; added a Citations line and cross-links to `buy-side-liquidity.md`/`sell-side-liquidity.md` for layer 7.
- **`buy-side-liquidity.md`, `sell-side-liquidity.md`** — before writing, verified both files already fully enumerate PDH/PWH/PMH and session highs/lows (added during the FVG ingest), so per `advisor()` guidance no dedicated Old High/Low file was warranted — this chapter's own material is otherwise recap. The one genuinely new item, the p.242 multi-swing approach-structure preference (Lower-Highs/Lower-Lows into an Old High, mirrored Higher-Highs/Higher-Lows into an Old Low, before treating the eventual sweep as high-conviction), was added symmetrically to both files' Formal Criteria, Common Mistakes, and Citations, cross-linked to the new Three Drive file.
- **`mitigation-block.md`** — one Common Mistakes note + cross-link for the p.258 fallback: a failed Three Drive isn't a dead setup, it's a plain Failure Swing that falls back to Mitigation Block as the next pd-array-stack layer.
- **`fvg-setup-checklist.md`** — reciprocal Common Mistakes note + cross-link disambiguating it from the new Three Drive file (same trigger triad, no approach-structure requirement).
- **`GLOSSARY.md`** — added "Rising Wedge / Falling Wedge" and "Three Drive" entries (both flagged non-ICT).

**Out of scope (third consecutive ruling for this source's SMT tangent):** the SMT Divergence / Pair Divergence / correlated-pairs section (pp.245–251) — `concepts/16-smt-divergence/` already has five files including `correlated-pairs-smt.md`, and the author himself frames this section as a preview of material covered elsewhere ("ส่วนหนึ่งของเนื้อหาของ SMT Divergence"). The p.243 HTF-bias-inheritance diagram and p.244 IRL/ERL walk were also skipped as recap of `top-down-analysis.md` and the existing IRL/ERL pair, matching the same call made on IRL/ERL during the Order Block ingest.

This is the **last chapter of the PD Array Matrix series** — all 7 pd-array-stack layers have now been independently confirmed via diagram in all 7 chapters of the same source book. Updated `SOURCES.md`, `INDEX.md` (1 new file), `TIMELINE.md` (2026 Year-Refined + Community-Attributed sections). Ran a background lint pass (structure, links, `## ICT vs Community` presence on the new file, alias/Source-ID set consistency, JSON/prose `related[]` mirroring on all new cross-links) before presenting for review.

## [2026-08-08] ingest | Thai Community chapter — ICT: Turtle Soup Pattern

Ingested "ICT Turtle Soup.pdf" (13 pages, pp. 262–273; 30MB, under the Read tool's 100MB limit, read directly). New Source ID `THAI-COMMUNITY-2026-TURTLE-SOUP` — a **different, standalone chapter** from the same author (page numbering continues immediately from the PD Array Matrix series' final chapter, but the title framing changes, matching the "ICT Technic Trade in FVG" pattern). Ran a background research survey across 8 candidate terms before writing; the survey's core finding — Turtle Soup, Judas Swing, Power of Three, IPDA/True Day Open, and Equal Highs/Lows are all already thoroughly documented at `confidence: high` with ICT sourcing — was put to `advisor()` alongside 5 scope questions. Verdict: **no new files**, enrichment-only ingest. 0 new files, 6 edits:

- **`turtle-soup.md`** — added 2 new worked Examples (Equal Low sweep + FVG + equilibrium-rebalance entry, pp.271–272; the mirrored Equal High/−FVG bearish case, p.273); a Common Mistakes note disambiguating this file's sweep+**close-back-inside** requirement from `fvg-setup-checklist.md`'s sweep+displacement+FVG triad (no close-back-inside requirement there); a Common Mistakes note on the source's claim that Turtle Soup and Judas Swing are the same underlying behavior, differing only by whether the sweep falls in the 02:00–05:00 NY window (p.269); and a note on the source's stated preference for pairing Turtle Soup with Power of Three context (p.262).
- **`judas-swing.md`** — reciprocal Common Mistakes note + cross-link for the Turtle Soup/Judas Swing timing-window equivalence.
- **`power-of-three.md`** — new cross-link to `turtle-soup.md` (previously PO3 only linked to `judas-swing.md`), plus a Common Mistakes note on the source's Turtle-Soup-as-Manipulation-phase-signature framing.
- **`stop-hunt-pattern.md`** — added "Stop Raid" as an explicit alias (top-matter + JSON), cited directly from the source's own text (p.263, stated alongside BSL/SSL as a synonym for Stop Hunt).
- **`equal-highs.md`, `equal-lows.md`** — one Common Mistakes line each, cross-linking to `turtle-soup.md`'s new worked examples, without adding sweep/FVG/MSS content to these definitional files themselves (confirmed via research survey that they correctly scope to "what EQH/EQL are," not entry technique).
- **`GLOSSARY.md`** — new "Quasimodo / QM" entry under a new `## Q` section (non-ICT, out of scope per the source's own one-sentence passing comparison, p.262 — not enough content for a dedicated file, consistent with the prior Breaker Block ingest's Quasimodo exclusion noted in this same log); new "Stop Raid" entry under `## S`.

**Explicitly out of scope, checked against existing coverage before excluding:**
- **Quasimodo/QM** — one comparative sentence (p.262), not a treatment; no Formal Criteria to source. GLOSSARY entry only, no file.
- **Relative High / Relative Low** — diagram labels only (pp.269–270), no prose definition. Skipped rather than added as aliases to `swing-high.md`/`swing-low.md`, since `relative-equal-highs-lows.md` already owns "relative" to mean near-equal-tolerance levels — stamping the same word onto swing points would create a naming collision the wiki doesn't currently have.
- **p.264 Market Maker rationale, p.265 three-zone framing, p.267 EQH-first/EQL-first manipulation-side observation, p.268 big-picture premium/discount placement, p.266 Asian-range→London-sweep→OTE walk** — all recap of `stop-hunt-pattern.md`, `liquidity-sweep.md`, `london-judas-swing.md`, and existing premium/discount material; skipped to avoid citation-only appends.
- **p.270's 7 entry-method menu** — all already-documented mechanics (MSS+displacement, TDO, killzone, breaker, opposite-side PD array target). Checked the 5–10 pip wick-buffer stop against `stop-placement-by-pd-array.md`, which already carries a 2–5 pip default, a 2-candle convention, and a 10–20 pip alternative from the Order Block ingest — a fourth overlapping number wasn't worth adding.
- **Confidence deliberately left untouched on every file.** The "ลุงไมเคิล ... จาก YouTube" (Uncle Michael, from YouTube) citation on p.266 is the same secondary-pointer pattern as the Rejection Block ingest's Month-04 citation and the Order Block ingest's CISD citation — restates rather than resolves existing `high`-confidence ICT sourcing.

Updated `SOURCES.md` and `TIMELINE.md` (2026 Year-Refined section only — no Community-Attributed entry, since no new file was created). No `INDEX.md` changes. Ran a background lint pass (structure, links, JSON validity, alias/Source-ID set consistency, JSON/prose `related[]` mirroring on all new cross-links, GLOSSARY alphabetical placement) before presenting for review.

## [2026-08-09] ingest | Thai Community chapter — ICT Judas Swing: Market Maker Trap

Ingested "ICT Judas Swing.pdf" (6 pages, cover + pp. 275–279; 15MB, read directly). New Source ID `THAI-COMMUNITY-2026-JUDAS-SWING` — a third, standalone chapter by the same author, page numbering continuing from the Turtle Soup chapter but under yet another distinct title. Read `london-judas-swing.md`, `true-day-open.md`, and `ipda-reference-points.md` before writing; confirmed via `advisor()` that the London Judas KZ timing, Asia-range sweep, FVG entry, IRL/ERL targeting, and the 6-step trading method are all already documented — the source's checklist is `london-judas-swing.md`'s own Formal Criteria restated as a procedure, not new combination content (the `fvg-setup-checklist.md` precedent doesn't transfer here). 0 new files, 3 edits:

- **`london-judas-swing.md`** — added the source's "three-session-overlap" rationale (p.275) as a new Definition paragraph: the 00:00–05:00 NY Judas window exists because NY midnight (IPDA/TDO reset), London open, and Tokyo/Asian close cluster together — a causal explanation for an already-documented time window that wasn't previously stated anywhere in the library. Also added iFVG as an equally-used entry array alongside plain FVG, per the source's own worked M15 charts (pp.276, 279), cross-linked to `inversion-fvg.md`.
- **`true-day-open.md`** — reciprocal Common Mistakes note + cross-link: TDO's midnight reset anchors the three-session-overlap rationale.
- **`inversion-fvg.md`** — reciprocal cross-link to `london-judas-swing.md` for the iFVG-as-Judas-entry-array usage.

**Deliberately not cited:** the source's own clock figures for the London-open and Tokyo-close components of the overlap (p.275: "บ่ายสองโมง"/"บ่ายสี่โมงเย็น," literally 2pm/4pm NY) are internally inconsistent with the same source's later, more specific "ตี2 - ตี5" (2am-5am) statement on p.278 — read as a transcription artifact rather than a substantive claim, mirroring the Rejection Block ingest's precedent of leaving OCR-ambiguous numerics uncited rather than asserting an unverified figure. Only the causal claim (three session boundaries overlapping explains the window) was kept, not the specific clock numbers.

**Explicitly out of scope:** the 6-step method itself (Asia swing H/L, midnight-open reference, price-vs-midnight check, run to Asia High as ERL + Displacement, IRL entry search, DOL-ladder targets) — fully pre-existing across `london-judas-swing.md`, `true-day-open.md`, `asian-range.md`, and the IRL/ERL pair (already ruled recap during the Order Block ingest); the False Breakout/Stop Hunt synonym line (already on `turtle-soup.md`'s aliases); and the "Market Maker Trap" subtitle framing (grepped repo-wide, zero hits, but `stop-hunt-pattern.md`'s existing Definition already covers the deceptive-intent framing — not distinct enough to warrant its own alias entry).

Updated `SOURCES.md` and `TIMELINE.md` (2026 Year-Refined section only — no Community-Attributed entry, no new file). No `INDEX.md` or `GLOSSARY.md` changes. Ran a background lint pass (JSON validity, Source-ID top-matter/JSON mirroring, bidirectional `related[]` cross-link mirroring, repo-wide citation grep) before presenting for review.

## [2026-08-09] ingest | Thai Community chapter — ICT: Optimal Trade Entry (OTE)

Ingested "ICT Optimal Trade Entry_OTE.pdf" (13 pages, cover + pp. 281–292; 32MB, read directly). New Source ID `THAI-COMMUNITY-2026-OTE` — a fourth, standalone chapter by the same author, page numbering continuing from the Judas Swing chapter under yet another distinct title. Read `ote-overview.md`, `ote-705.md`, `ote-rules.md`, `fib-vs-ote.md`, and `ict-fib-overview.md` before writing; confirmed via `advisor()` that every OTE level in the source (0.5 EQ, 0.62, 0.705 "best", 0.79, plus the -0.27/-0.62/-1/-2 target set) is already documented at `confidence: high` with ICT-2017-OTE sourcing. 0 new files, 4 edits:

- **`ict-fib-overview.md`** — added the source's swing-selection procedure (p.285): anchor Fibonacci to the *prior completed period's* low→high range on the timeframe being traded (a Monday day-trade uses Friday's daily range, Tuesday uses Monday's; a new week uses last week's range; a new month uses last month's), drilling into swings inside that range only after it's set. Framed as one reasonable default, not a strict gate — the source's own later worked example (p.288, measuring within the current month rather than from the prior month's low) doesn't follow its own rule, so that example was left uncited rather than treated as a contradiction to resolve.
- **`pd-array-stack.md`** ↔ **`ote-overview.md`** — new bidirectional cross-link, per the author's own closing observation (p.292) that where price rests within an OTE retracement often corresponds to which pd-array-stack layer it's landing on. This is the eighth chapter of the same book to touch the stack, but as a prose cross-reference, not a diagram — the "seven diagrams / every chapter" count language in `pd-array-stack.md` was deliberately left untouched, since that count is specifically about stack-order diagram confirmations.
- **`ote-overview.md`** — added a Common Mistakes note that the source uses the same OTE zone in both a reversal context (anchored to a Market Structure Shift) and a continuation context (anchored to a Break of Structure), pairing either with Breaker/FVG confluence inside the zone (p.291).
- **`fib-vs-ote.md`** — added the source's stated rationale (p.281) for why classical sub-50% fib levels (0.236, 0.382) are absent from the ICT/OTE set: OTE runs on the premium/discount principle, so anything shallower than equilibrium is structurally the wrong side of the range for an entry.

**Explicitly out of scope:** the standard 3-candle fractal Swing High/Low definition (pp.286–287) — verbatim match to `swing-high.md`/`swing-low.md`, no new content; four worked chart examples across multiple timeframes and instruments (pp.283–284, 288–290) — illustrative recap of already-documented OTE mechanics; a one-off "-127.00%" target level appearing in a single worked example (p.289) — absent from both the source's own stated Level table (p.281) and its Fibonacci-tool checkbox screenshot (p.282), read as an eyeballed one-off rather than a systematic addition, not worth diluting the existing curated `standard-deviation-projections.md` set; the author's own observation that the 62%/61.8% difference and the 78.6-88.6% alternate zone are practically immaterial — already covered by `fib-vs-ote.md`'s existing classical-vs-ICT contrast; and the author's closing personal preference for Standard-Deviation-Projection-style Fibonacci over OTE-retracement-style — a taste remark, not a claim about either tool. As with every prior chapter of this book, the "ลุงไมเคิล ... YouTube" (Uncle Michael, from YouTube) attribution (p.282) was treated as a secondary pointer, not new ICT-attribution evidence — no confidence changes anywhere.

Updated `SOURCES.md` and `TIMELINE.md` (2026 Year-Refined section only — no Community-Attributed entry, no new file). No `INDEX.md` or `GLOSSARY.md` changes. Ran a background lint pass (JSON validity, Source-ID top-matter/JSON mirroring, bidirectional `related[]` cross-link mirroring including the pd-array-stack↔ote-overview pair, repo-wide citation grep) before presenting for review.

## [2026-08-09] ingest | Thai Community chapter — ICT Smart Money Reversal

Ingested "ICT Smart Money Reveral.pdf" (12 pages, cover + pp. 294–304; 28MB, read directly). New Source ID `THAI-COMMUNITY-2026-SMART-MONEY-REVERSAL` — a fifth, standalone chapter by the same author, page numbering continuing from the OTE chapter under yet another distinct title. Read all 4 files in `concepts/16-smt-divergence/` (`smt-divergence.md`, `correlated-pairs-smt.md`, `index-smt.md`, `smt-confirmation.md`) before writing, plus a targeted grep for "SMR," "Smart Money Reversal," "DXY," "BTC," "ETH," and "myfxbook" across the directory and `GLOSSARY.md`. Confirmed via `advisor()` that the core SMT mechanic (two correlated assets, one sweeps a level and the other fails to confirm, signaling reversal) is already documented at `confidence: high` with ICT-2017/2022 sourcing — this chapter is a rename plus cross-market examples, not new mechanics. 0 new files, 2 edits:

- **`smt-divergence.md`** — added "Smart Money Reversal," "SMR," and "SMR Divergence" to Aliases (top-matter + JSON), per the author's own explicit closing statement (p.304) that SMR is what ICT-Concept practitioners now call the same Correlation + Divergence technique he already used before learning it as ICT.
- **`correlated-pairs-smt.md`** — added a new Example: BTC/ETH as a crypto symmetrical-correlation pair (p.300), the same technique applied outside FX with no modification; a Common Mistakes note that JPY-cross groupings (EUR/JPY vs GBP/JPY) correlate less consistently than USD-quoted pairs and should be verified before relying on them (p.301); and a Myfxbook mention as a concrete free correlation-checking tool (1-day/1-week/1-month windows), tied to the file's existing `corr_60d > 0.7` threshold which previously had no suggested verification method (pp.301–303).
- **`GLOSSARY.md`** — added an "SMR" entry under `## S`, alphabetically between "SMC" and "SMT."

**Explicitly out of scope:** the Symmetrical Correlation / Inverse Correlation two-label taxonomy (pp.298–299) — `smt-divergence.md`'s Definition already names "EURUSD vs DXY (inverse)" and `correlated-pairs-smt.md`'s Common Mistakes already distinguishes positive from strong-negative pairings; only the two-label packaging was new, and a naming layer without new mechanics wasn't added. The pp.297–298 sequencing advice ("wait for the swept pair to displace, then trade the non-swept pair") was also left out — the source contradicts itself on which pair to trade (p.297: buy the sweeper, EURUSD, after its own confirmation; p.298: the reversal strength belongs to the non-sweeper, GBPUSD, because it shows a Failure Swing) — no rule was extracted from an internally inconsistent source, mirroring the Judas Swing ingest's precedent of leaving unreconciled source disagreements uncited. Also skipped as pre-existing recap: DXY-inverse examples (pp.299, 302, already in `smt-divergence.md`); ES/NQ indices (p.302, `index-smt.md` already covers all four US indices with a reliability table); AUDUSD/NZDUSD (p.301, already enumerated in `correlated-pairs-smt.md`); and the p.295 "SMR supports conviction, isn't the entry trigger" framing (already `smt-confirmation.md`'s entire Definition). As with every prior chapter of this book, the "ลุงไมเคิล ... YouTube" (Uncle Michael, from YouTube) attribution (p.304) was treated as a secondary pointer, not new ICT-attribution evidence — no confidence changes anywhere; `smt-divergence.md` and `correlated-pairs-smt.md` remain `high`.

Updated `SOURCES.md`, `TIMELINE.md` (2026 Year-Refined section only — no Community-Attributed entry, no new file), and `GLOSSARY.md` (1 new entry). No `INDEX.md` changes. Ran a background lint pass (JSON validity, Source-ID top-matter/JSON mirroring, alias-set consistency between top-matter and JSON, GLOSSARY alphabetical placement, repo-wide citation grep) before presenting for review.

## [2026-08-09] ingest | Thai Community chapter — Liquidity In Trading & Draw On Liquidity

Ingested "ICT Liquidity in Trading.pdf" (24 pages, cover + pp. 306–328; 62MB, under the Read tool's 100MB limit but over the 20-page-per-call cap, read directly in 2 calls). New Source ID `THAI-COMMUNITY-2026-LIQUIDITY-TRADING` — a sixth, standalone chapter by the same author, page numbering continuing from the Smart Money Reversal chapter under yet another distinct title. Dispatched a background research survey across 14 candidate topics against all 14 files in `concepts/02-liquidity/` before writing anything; the survey's core finding — BSL/SSL, Equal Highs/Lows, Trendline Liquidity, Draw on Liquidity, Internal/External Range Liquidity, and the PD-array-stack composition are all already thoroughly documented at `confidence: high` — was put to `advisor()` alongside 4 scope questions. Verdict: **no new files**, enrichment-only ingest. 0 new files, 4 edits:

- **`top-down-analysis.md`** — added the source's "Major / Medium / Minor Liquidity" three-tier classification (Session/Daily/Weekly/Monthly swings = Major, 15m/1H = Medium, 1m/5m = Minor, pp.314–316), citing the author's own observation (p.315) that this is Top-Down Analysis applied to liquidity levels rather than entries — restates the file's existing TF-pairing rule (paired TFs must stay one full step apart) under liquidity-specific vocabulary, not a new rule. Cross-linked to `buy-side-liquidity.md`/`sell-side-liquidity.md`.
- **`buy-side-liquidity.md`, `sell-side-liquidity.md`** — added a symmetric new Example each: round-number liquidity clustering, worked with the source's own gold instance (1900/1925/1950/1975/2000, p.320) — both files already listed "round-number levels" in Formal Criteria but had no worked example.
- **`stop-hunt-pattern.md`** — added a Common Mistakes note that "Bull Trap"/"Bear Trap" (pp.321–322) are pre-ICT retail terms the author used before learning ICT for this same fake-breakout-then-reverse behavior, not a distinct pattern; the source's only actionable fix (raise the analysis timeframe from M1–M15 to H4/D1/W1) is `top-down-analysis.md`'s existing premise, cross-linked there.
- **`GLOSSARY.md`** — added "Bear Trap" and "Bull Trap" entries (both flagged non-ICT), alphabetically placed among the existing B-section acronyms.

**Explicitly out of scope, checked against existing coverage before excluding:**
- **Range Liquidity** (p.316) — the source's own definition (swing highs/lows bounding a sideways range, which sweep and return to equilibrium) is verbatim `dealing-range-equilibrium.md`'s existing definition; grepped `concepts/27-equilibrium/` and confirmed before excluding — only the "Range Liquidity" naming was new, no new mechanic, matching the Symmetrical/Inverse Correlation precedent from the Smart Money Reversal ingest.
- **Fibonacci levels as liquidity clustering** (p.320, listed alongside round numbers) — the source gives no mechanism for why a retracement ratio would hold resting stops, and `fib-vs-ote.md` already documents 78.6%/88.6% specifically as the *classical* alternate zone the ICT/OTE set doesn't use — asserting a clustering claim here would contradict that file's existing reasoning rather than extend it. Only the round-number half of the claim was kept.
- **"Gap in price" from news events** (p.328) — two sentences, no formal criteria (a news event causes a chart gap that price later fills); `gap-classification.md` exists specifically to disambiguate FVG/Volume Imbalance/Liquidity Void, and adding a fifth gap flavor with no criteria would work against that file's purpose.
- **A root "Liquidity" overview file** (pp.306–313) — considered and rejected via `advisor()`: the retail-vs-Smart-Money framing, the 20-baht stock walkthrough, and the "liquidity = reversal point or price target" framing are all already distributed across `stop-hunt-pattern.md`, `liquidity-pool.md`, `liquidity-sweep.md`, and `draw-on-liquidity.md`, with `liquidity-pool.md` already self-describing as the umbrella term. The absence of a root file is a deliberate shape, not a hole.
- **Trendline Liquidity, EQH/EQL, DOL/"Run on Liquidity", IRL/ERL, PD-array-stack composition inside IRL** — all pure recap of `trendline-liquidity.md`, `equal-highs.md`/`equal-lows.md`, `draw-on-liquidity.md`/`liquidity-run.md`, `internal-range-liquidity.md`/`external-range-liquidity.md`, and `pd-array-stack.md` respectively (IRL/ERL is now the fourth consecutive out-of-scope ruling for this pair across this window's ingests).
- **Order Block needs MSS+Displacement before entry** (p.328) — already `order-block-criteria.md`'s existing rule. **Market Maker / FOREX-MM-isn't-"เจ้า" discussion** (pp.318–319) and the **GBPUSD session-open walk** (p.320) — trader-education framing and recap of `london-judas-swing.md`/`asian-range.md`, no new criteria.

Updated `SOURCES.md`, `TIMELINE.md` (2026 Year-Refined section only — no Community-Attributed entry, no new file), and `GLOSSARY.md` (2 new entries). No `INDEX.md` changes. Ran a background lint pass (JSON validity, Source-ID top-matter/JSON mirroring across all 4 files, alias-set consistency, symmetric BSL/SSL edit check, GLOSSARY alphabetical placement, repo-wide citation grep) before presenting for review.

## [2026-08-09] ingest | Thai Community chapter — Internal Range Liquidity & External Range Liquidity: Simply Concept

Ingested "ICT Int_Ext Range Liquidity.pdf" (40 pages, cover + pp. 330–368; 98MB, just under the Read tool's 100MB limit, read directly in 2 calls). New Source ID `THAI-COMMUNITY-2026-IRL-ERL` — a seventh, standalone chapter by the same author, page numbering continuing from the Liquidity In Trading chapter under yet another distinct title (running header reads "Draw On Liquidity"). This is a full ~39-page chapter dedicated entirely to IRL/ERL — a topic already ruled recap in passing during 4 prior ingests in this window (Order Block, Old High/Low, Liquidity In Trading, and once more within this same chapter's own overlap with Draw on Liquidity). Dispatched a background research survey across 9 candidate topics against `internal-range-liquidity.md`, `external-range-liquidity.md`, `pd-array-stack.md`, `draw-on-liquidity.md`, and `liquidity-run.md` before writing anything; results were put to `advisor()` alongside one explicit verification instruction (check `price-delivery-cycle.md` before ruling on the chapter's named 3-step cycle). 0 new files, 2 edits:

- **`internal-range-liquidity.md`** — added Liquidity Void to the Formal Criteria's IRL enumeration (previously listed only internal swings, FVGs, order blocks, and internal trendline liquidity — a genuine gap, since the source names Liquidity Void explicitly three separate times: p.332 prose, and PD-array-stack graphics on pp.345–346, and `liquidity-void.md` already exists as a documented array type in this wiki).
- **`inversion-fvg.md`** — added a new Common Mistakes note: when a Displacement-direction engulfing candle destroys an FVG a trader is holding as an IRL target, the correct read is defensive (stop out, since price may run to the opposite ERL instead), not an inversion setup (p.363). This is distinct from the file's existing iFVG framing, where a traded-through FVG flips polarity and becomes a fresh reversal entry — the source's reading is the risk-management counterpart to that optimistic case, not a contradiction of it.

**Explicitly out of scope, checked against existing coverage before excluding:**
- **The chapter's own named 3-step cycle** (sweep ERL → rebalance IRL via Fibonacci-to-Equilibrium → continue to next DOL, p.368) — per `advisor()`'s explicit instruction, read `price-delivery-cycle.md` in full before ruling: its Reversal/Retracement state-machine branches already encode this exact loop (Reversal raids the opposing liquidity pool → Expansion resumes → decays to a new Consolidation; Retracement rebalances the FVG/Liquidity Void left by displacement → Expansion resumes → Consolidation). Only the chapter's own packaging (a named "External↔Internal" cycle) was new; adding a second named cycle for the same mechanic in a different directory would invite exactly the confusion `price-delivery-cycle.md` exists to prevent.
- **Nial Fuller's Inside Bar / Mother Bar concept mapped to ERL/IRL** (pp.364–366) — the underlying Mother-Bar/Inside-Bar structure with a 50%-Equilibrium midpoint is already documented on `displacement-strength-criteria.md` and `equilibrium-definition.md`, cited to `THAI-COMMUNITY-2026-FVG` pp.101–106. Only the Nial Fuller attribution and the explicit ERL/IRL relabeling of Mother Bar high/low and Inside Bar are new — a naming layer without new mechanics.
- **The wick=ERL / body=IRL candlestick mnemonic** (p.349) — not actionable on its own, and collides with `liquidity-void.md`'s existing, unrelated use of wick-vs-body for void-boundary drawing conventions; stamping a second meaning on the same two words in the same directory was judged not worth the ambiguity.
- **Fibonacci explicitly recommended to verify an IRL rebalance reaches Equilibrium** (p.368) — composes two already-documented facts (`ict-fib-overview.md`'s 0.50 = EQ, `dealing-range-equilibrium.md`'s EQ as the premium/discount reference) with no new threshold or procedure; second consecutive chapter in this window attaching a loose Fibonacci claim to a liquidity concept (after the Liquidity In Trading ingest's round-number/Fib clustering claim).
- **Fractal/nested internal liquidity within one external range** (p.367) — `liquidity-matrix.md` already covers multi-TF liquidity-pool inventory and `pd-array-stack.md` already notes nested lower-TF stacks inside a single HTF layer; the source's contribution is one annotated chart with no new criteria for when nesting matters.
- **A "Displacement required or you're trading Aggressive" warning** (p.362) — `fvg-setup-checklist.md` already makes Displacement a mandatory gate for FVG entries; a label for violating an already-mandatory rule isn't new content.
- **A Weekly-wick-as-BSL/SSL, wait-for-H1-sweep-then-MSS+Displacement+FVG recipe** (p.349) — the same TF-pairing rule as `top-down-analysis.md`'s existing Weekly→H4/H1 pairing plus `fvg-setup-checklist.md`'s entry trigger, instantiated at one specific TF pair — the same shape as last ingest's Major/Medium/Minor Liquidity tiers, with numbers filled in rather than a new rule.
- **Buy/Sell Model diagrams, the "price only does 2 things" framing, and eight worked chart walkthroughs** (pp.337–348, 351–360) — illustrative recap of `draw-on-liquidity.md`'s existing Definition and the IRL/ERL mechanics already covered elsewhere; the p.343 Confirm-Swing-Low-before-entry point is `swing-low.md` plus existing sweep-confirmation material.

Updated `SOURCES.md` and `TIMELINE.md` (2026 Year-Refined section only — no Community-Attributed entry, no new file). No `INDEX.md` or `GLOSSARY.md` changes. Ran a background lint pass (JSON validity, Source-ID top-matter/JSON mirroring, alias-set consistency, `related[]` mirroring on both new cross-links, repo-wide citation grep, cross-file ordinal consistency check) before presenting for review.

## [2026-08-10] ingest | Thai Community chapter — Daily Bias & Close Price

Ingested "ICT Daily Bias_Close Price.pdf" (11 pages, cover + pp. 401–411; 24MB, read directly in a single call). New Source ID `THAI-COMMUNITY-2026-DAILY-BIAS-CLOSE` — an eighth, standalone chapter by the same author, but page numbering is *not* contiguous with the prior chapter (which ran through p.368): pp.369–400 was speculated at the time to belong to a single un-ingested "Daily Bias Method 1" chapter, since this chapter opens by explicitly labeling itself "Daily Bias แบบที่ 2" (Method 2) — **correction, see the Market Maker Model ingest below**: that speculation was wrong for at least part of the range, since pp.370–386 turned out to be the "Market Maker Model (MMXM)" chapter, not a Daily Bias variant. Personally read `concepts/25-htf-bias/daily-bias.md` in full before scoping (not just via the survey agent's report), confirming it determines bias only via daily structure (BOS/CHoCH) + EQ position + DOL direction — no close-vs-prior-day-range mechanic existed anywhere in the file. Dispatched a background research survey across 6 candidate topics against `daily-bias.md`, `buy-side-liquidity.md`/`sell-side-liquidity.md`, `stop-hunt-pattern.md`/`turtle-soup.md`/`liquidity-sweep.md`, `displacement-strength-criteria.md`/`equilibrium-definition.md`, `ict-fib-overview.md`, and a repo-wide grep for "OLHC"/"OHLC" before writing anything; results were put to `advisor()`. Unlike the six prior enrichment-only ingests, this chapter fills a genuine gap in a file's core purpose. 0 new files, 2 edits:

- **`daily-bias.md`** — added the source's close-vs-PDH/PDL method as a second, explicitly-labeled community-attributed alternative to the existing structure/EQ/DOL method: close above PDH → bullish next-day bias; close below PDL → bearish; sweep-PDH-then-close-back-below → bearish reversal; sweep-PDL-then-close-back-above → bullish reversal; close inside the prior day's range → no bias, don't trade. Also added the source's operational caution (p.403) against inferring a bias from the daily candle's visual shape before price actually reaches the PDH/PDL level in question, as a new Common Mistakes bullet. Cross-linked to `stop-hunt-pattern.md` (the reversal branches' underlying mechanic) and `buy-side-liquidity.md`/`sell-side-liquidity.md` (PDH/PDL as reference levels). `Year Refined` 2022→2026.
- **`stop-hunt-pattern.md`** — added a reciprocal cross-link and Citations line noting this source's application of the sweep-then-fail-to-close-through mechanic at daily granularity for next-day Daily Bias (pp.408–410) — the file already documented the mechanic itself; this is its first daily-close/next-day-output framing.

**Explicitly out of scope, checked against existing coverage before excluding:**
- **"OLHC"/"OHLC" labels** (pp.406–407) — zero existing repo hits looked like a clean gap, but these are candle Open/Low/High/Close orderings describing which extreme printed first, not ICT or community terminology; "OHLC" is universal chart-data notation, so a GLOSSARY entry defining it as a bias pattern would actively mislead. The five branches were documented by what they are; the labels were left out.
- **Fibonacci-on-prior-day's-range for entry** (p.405) — confirmed via the survey that `ict-fib-overview.md` already carries this near-verbatim, sourced to `THAI-COMMUNITY-2026-OTE` p.285 — third consecutive chapter restating this same author's own earlier Fibonacci convention.
- **"May take 2 days to reach the Draw on Liquidity" observation** (pp.408, 410) — one worked example, not a stated rule; the underlying sweep-then-return-to-a-lower-TF-PD-array mechanic is already `price-delivery-cycle.md`'s Retracement branch plus `draw-on-liquidity.md`.
- **The source's own Mother Bar/Inside Bar naming for the close-inside-range branch** (p.411) — the wiki already uses that exact term pair on `displacement-strength-criteria.md`/`equilibrium-definition.md` for a different purpose (locating an entry point within an already-qualified displacement candle, 50%=Equilibrium). To avoid conflating the two applications of the same vocabulary, the "close inside prior range → no trade" rule was written standalone rather than under the Mother Bar/Inside Bar label; it's mentioned only as a one-clause parenthetical noting the source frames it that way, with a cross-link to `equilibrium-definition.md`.
- **The four worked chart examples** (pp.404–405, 410) — illustrative only.

Updated `SOURCES.md` and `TIMELINE.md` (2026 Year-Refined section only — eighth standalone chapter, no Community-Attributed entry, no new file). No `INDEX.md` or `GLOSSARY.md` changes (the OLHC/OHLC skip above was the main candidate and was ruled out as actively misleading). Ran a background lint pass (JSON validity, Source-ID top-matter/JSON mirroring across both edited files, `related[]` JSON/prose mirroring on both new cross-links, repo-wide citation grep, cross-file ordinal consistency check between SOURCES.md and TIMELINE.md) before presenting for review.

## [2026-08-10] ingest | Thai Community chapter — Market Maker Model (MMXM)

Ingested "ICT MMXM.pdf" (18 pages, cover + pp. 370–386; 54MB, under the Read tool's 100MB/20-page limits, read directly in a single call). New Source ID `THAI-COMMUNITY-2026-MMXM` — a ninth ingested chapter from the same author. Page numbering (pp.370–386) fills part of the pp.369–400 gap the Daily Bias ingest above had speculated was a single unread "Daily Bias Method 1" chapter — that speculation was wrong for this portion; corrected the Daily Bias entries in `SOURCES.md`, `TIMELINE.md`, and this file's own prior entry above.

Personally read `concepts/12-power-of-three/power-of-three.md` before scoping, confirming it already asserts MMBM/MMSM as ICT-original terms in one sentence but develops neither the 6-state cycle nor the two-curve structure. Dispatched a background research survey across 8 candidate topics — `power-of-three.md`/`accumulation-phase.md`/`manipulation-phase.md`/`distribution-phase.md`/`amd-vs-po3.md`/`amd-on-htf.md`, `smt-divergence.md`, `price-delivery-cycle.md`, `cisd.md`, `unicorn-model.md` (template), `dealing-range.md`/`dealing-range-equilibrium.md`, `three-drive-pattern.md`, and a GLOSSARY.md grep — before writing anything; results were put to `advisor()`. Verdict: unlike seven of the eight prior ingests this window, this one is **not** enrichment — it's a genuine gap in a framework the wiki names but never built out. 1 new file, 3 edits:

- **`concepts/31-models/market-maker-model.md`** (new) — the 6-state MMXM cycle (Original Consolidation, Accumulation/Re-Accumulation, Smart Money Reversal, Distribution/Re-Distribution) in both MMBM and MMSM directions; the composition rule (≥2 states of BISI/+OB or SIBI/-OB left behind and rebalanced before continuing to the opposite HTF pool); the invalidation gate stated explicitly by the source (a valid MMBM requires price not break the Accumulation state's low — the actual validity test, not visual resemblance to the diagram); the two SMR entry setups (MSS+Displacement with a CISD-confirmed nested FVG, or Turtle Soup) and two SMR verification routes (Classic Divergence → routes to −FVG/−OB above MSS or a Breaker Block; Failure Swing → routes to the Mitigation Block specifically); and the HTF/LTF pairing rule for zone selection. One worked example kept (EURUSD H1, late Jan 2024) — specifically because it carries the invalidation-gate ruling in the source's own words. `ICT Confidence: high`, `Year Introduced: 2020` (source dates ICT's own YouTube explanation to late 2020), `Year Refined: 2026`.
- **`power-of-three.md`** — added a link from its existing MMBM/MMSM sentence to the new file; sentence itself left unchanged per scope discipline (PO3 stays the 3-phase file).
- **`cisd.md`** — added a new Formal Criteria point: when a CISD/OB is confirmed by a nested FVG (the MMXM chapter's SMR setup), price should not enter that FVG beyond 50% before multiple retests — a different depth rule from the file's existing "50% of the CISD candle" entry point, which refers to a different object.
- **`smt-divergence.md`** — added "Classic Divergence" as an alias (the source's name for the cross-pair verification this file already documents) and a cross-link to the new file.

**Blocking question resolved before writing:** per `advisor()`'s explicit instruction, read `mitigation-block.md` in full before treating the MMXM chapter's "Failure Swing" SMR-verification method as a naming collision. Confirmed it's the *same* Failure Swing already documented there and in GLOSSARY.md (price fails a new HH/LL, then structure breaks the other way) — the MMXM chapter just names where to enter when this is the confirmation method (the Mitigation Block). No new GLOSSARY entry, no redefinition; the new file cross-links `mitigation-block.md` instead.

**Explicitly out of scope, checked against existing coverage before excluding:**
- **Rebalance-after-expansion / "price runs to Liquidity Pools" framing** (p.373) — the source itself frames this as Internal/External Range Liquidity restated; sixth consecutive chapter in this window landing on that same ground.
- **The DXY/GBPUSD SMT cross-check chart walkthroughs** (pp.383, 386) — `correlated-pairs-smt.md` already owns the cross-pair-selection technique; only the Classic-Divergence naming was novel (see `smt-divergence.md` edit above).
- **Three of the four real-chart worked examples** (EURUSD Daily MMSM p.382, the GBPUSD/EURUSD H1 comparison p.383, the EURUSD Daily technique walkthrough p.384, the DXY Daily example p.386) — kept only the EURUSD H1 MMBM example (p.380), since it's the one carrying the source's own invalidation-gate ruling; the others are illustrative restatements of the same mechanics.

Updated `SOURCES.md`, `TIMELINE.md` (both a new 2020-section note and a 2026-section new-file bullet, since `Year Introduced: 2020` and `Year Refined: 2026` differ), and `INDEX.md` (new-file entry under 31 — Models, placed after `ict-2024-model`). No `GLOSSARY.md` changes. Also corrected a self-discovered ordinal/speculation error from the Daily Bias ingest immediately above (the "pp.369–400 is Daily Bias Method 1" guess was wrong for the pp.370–386 portion) in `SOURCES.md`, `TIMELINE.md`, and this file. Ran a background lint pass (JSON validity and section-order for the new file, Source-ID top-matter/JSON mirroring across all 4 touched files, `related[]` JSON/prose mirroring, INDEX.md entry presence, repo-wide citation grep, cross-file ordinal consistency check between SOURCES.md and TIMELINE.md) before presenting for review.


## [2026-08-10] ingest | Thai Community chapter — Daily Bias & Order Flow

Ingested "ICT Daily Bias_Order Flow.pdf" (14 pages: 1 unnumbered cover + pp.388–400; 37MB, read directly in a single call). New Source ID `THAI-COMMUNITY-2026-DAILY-BIAS-ORDER-FLOW` — a tenth ingested chapter by the same author. This chapter is the source's own "Daily Bias แบบที่ 1" (Method 1) and sits **earlier in the book** than the already-ingested `THAI-COMMUNITY-2026-DAILY-BIAS-CLOSE` ("Method 2," pp.401–411) despite being ingested after it — resolving the pp.387–400 gap that ingest and the MMXM ingest both flagged as unaccounted-for. With this chapter read, only p.369 remains an unread page in the book; not guessed at.

Dispatched a targeted grep+read survey (not a background agent, given the chapter's small size) across `concepts/03-order-flow/` (bullish/bearish-order-flow.md), `concepts/06-fair-value-gaps/inversion-fvg.md`, `concepts/25-htf-bias/` (daily-bias.md, top-down-analysis.md), `concepts/27-equilibrium/` (equilibrium-definition.md, equilibrium-as-decision-point.md), `concepts/05-pd-arrays/` (discount-array.md, pd-array-confluence.md), and `concepts/26-imbalance/imbalance-rebalance.md` before scoping. `advisor()` caught two issues before writing: (1) a Method-numbering collision I was about to introduce — writing a new "Method 1" block without touching the existing "Method 2" paragraph would have left `daily-bias.md` implying two contradictory definitions of "Method 1" (the wiki's own structure/EQ/DOL method vs. the source's Order-Flow method); fixed by stating the source's own three-method taxonomy explicitly and up front, disambiguated from the pre-existing method. (2) An enrichment I was about to drop as already-covered (the EQ-crossing PD-array selection filter, pp.396, 399) — I'd checked `equilibrium-as-decision-point.md` only and found it covers *which side* to trade, not the source's actual claim: *which array to pick among several same-side candidates*. Reading `discount-array.md` and `equilibrium-definition.md` confirmed neither states this narrower selection rule, so it was kept.

**Enrichment-only, no new file:**
- **`daily-bias.md`** — added the source's own explicit Method 1/2/3 taxonomy (Method 1 = Order Flow, this chapter; Method 2 = close-vs-PDH/PDL, already documented; Method 3 = past Swing Points, named p.389 but not elaborated by the source, so not documented further here), disambiguated from the wiki's pre-existing structure/EQ/DOL method which predates this source and isn't part of its numbering. Method 1 cross-links to `bullish-order-flow.md`/`bearish-order-flow.md` (no new criteria — the source's Order Flow definition maps directly onto both files' existing content) and `cisd.md` (the source recommends CISD over generic Order Blocks, calling CISD the only "Original" ICT order block, p.390).
- **`inversion-fvg.md`** — added "Flip Zone" as a new alias (top-matter + JSON, matching pattern) for the file's existing support-becomes-resistance mechanic; the chapter's pp.397–398 walkthrough (Bullish FVG broken → becomes Inverse FVG → price respects it → new Bearish Order Flow begins) is the identical mechanic already documented, just under a new name.
- **`pd-array-confluence.md`** — added an EQ-crossing selection filter as a new paragraph narrowing factor 7 ("fresh, unmitigated arrays preferred"): when several same-side unmitigated arrays exist in one dealing range, discard any that formed on the far side of Equilibrium even if price respected them, then prefer whichever survivor has confluence with the EQ 50% level — the source applies this to both a plain FVG selection (p.396) and to choosing among multiple respected Inverse-FVG candidates (p.399, "not every position, even if price respects the FVG"). `Year Refined` bumped 2025→2026.

**Explicitly out of scope, checked against existing coverage before excluding:**
- **pp.391–394, real-chart Order Flow worked examples** (MSS+Displacement, BISI/SIBI chains, Sweep→MSS+DIS cycles) — illustrative restatement of `bullish-order-flow.md`/`bearish-order-flow.md`'s existing Formal Criteria and Visual Pattern; not cited on those files per AGENTS.md's citation gate (source must genuinely change canonical understanding).
- **p.395, candle-body-must-close-through-not-just-wick** for judging FVG respect/violation — read against `fvg-mitigation.md` (covers touch/CE/full-fill thresholds, a different axis) and confirmed as the same close-vs-wick requirement `inversion-fvg.md` already states as its inversion-trigger criterion; here it's applied to continuation confirmation rather than the inversion trigger, same underlying rule.
- **p.400, "HTF at least 2 timeframes above LTF" and 3-TF (HTF/LTF/Entry) top-down method** — `top-down-analysis.md` already documents this exact TF-pairing discipline via its skip-a-rung table and the immediately-prior ingest's Major/Medium/Minor Liquidity tiers.
- **p.388, ERL/IRL-rebalance framing of Daily Bias** — recap of already-thoroughly-documented `internal-range-liquidity.md`/`external-range-liquidity.md` material.

Updated `SOURCES.md` (new entry, plus stale-gap corrections to the `THAI-COMMUNITY-2026-MMXM` and `THAI-COMMUNITY-2026-DAILY-BIAS-CLOSE` entries, which both claimed pp.387–400 unaccounted-for) and `TIMELINE.md` (2026 Year-Refined section only — no Community-Attributed entry, no new file). No `INDEX.md` or `GLOSSARY.md` changes ("Flip Zone" is not an abbreviation; GLOSSARY.md is abbreviation-only). Ran a background lint pass (JSON validity, Source-ID top-matter/JSON mirroring, alias-set consistency on `inversion-fvg.md`, `related[]` mirroring on all cross-links, repo-wide citation grep, cross-file ordinal consistency check) before presenting for review.

## [2026-08-10] ingest | Thai Community chapter — How to Use Daily Bias

Ingested "ICT How to Use Daily Bias.pdf" (16 pages, pp.412–427; 37MB, read directly in a single call). New Source ID `THAI-COMMUNITY-2026-DAILY-BIAS-HOW-TO-USE` — an eleventh ingested chapter by the same author, and the first in this run whose page numbering is contiguous with the immediately-prior ingest (`THAI-COMMUNITY-2026-DAILY-BIAS-CLOSE`, pp.401–411). p.369 remains the only unaccounted-for page in the book.

The chapter is a 12-part "Step by Step" worked example applying Method 2 (close-vs-prior-range, already documented) across two real weeks of EURUSD daily candles, plus a 5-point summary and a limitations section. Surveyed against `weekly-bias.md`, `weekly-quarters.md`, `manipulation-phase.md`, `displacement-strength-criteria.md`, `equilibrium-definition.md`, `equilibrium-as-decision-point.md`, and `bias-invalidation.md` before scoping. `advisor()` corrected two things before writing: (1) I was about to write a "close-vs-PWH/PWL" Formal Criteria line onto `weekly-bias.md` by inferring a specific threshold (prior week's high/low) that the source never actually states — it only says "this week's close is higher/lower than last week," ambiguous between close-vs-close and close-vs-high/low. This is the exact same speculation failure mode already hit twice on page-range guessing; caught before writing, so `weekly-bias.md` was left untouched and the generalization is recorded as an unelaborated claim only. (2) I had queued only the p.427 "breaks down in a clean multi-week trend" caveat as the one genuine gap and set aside p.421's sharper, more load-bearing point (a weekly check-stop overriding a 4-day daily-close streak) as "already covered by top-down discipline" — it isn't; `top-down-analysis.md` says higher TF constrains lower TF generically, not this specific claim (a same-direction daily-close cascade doesn't extend past a weekly candle that failed to break the prior week's range). Folded both into one paragraph.

**One addition, to `daily-bias.md`'s Method 2 block:** a weekly check-stop on the daily close-cascade — a same-direction daily-close streak (the source's own example: four straight lower-daily-lows) does not extend past a weekly candle that failed to itself close beyond the prior week's range; the source's own worked example (pp.421–422) stops the streak there and gets a sweep-then-reverse instead. Cross-linked to `bias-invalidation.md` for the underlying CHoCH/MSS-with-displacement mechanic. Same paragraph also records, as a claim only (no thresholds), the source's statement (p.412) that Method 2 generalizes to Weekly/Monthly/Yearly Bias, and that its worked example determines the week's bias first before cascading into daily.

**Explicitly out of scope, checked against existing coverage before excluding:**
- **pp.414–425, the 12 Step-by-Step chart walkthroughs** — illustrative application of Method 2, not new mechanics.
- **p.426 summary point 5** ("Tuesday/Wednesday tend to print the week's high or low") — already `weekly-quarters.md`'s Q2-manipulation/Q3-distribution map and its own "Tuesday lows on bullish weeks, Tuesday highs on bearish weeks" adage.
- **p.416** ("wait for ≥50% retracement of the breakout candle before trusting its direction") — already `equilibrium-definition.md`'s Engulfing/Marubozu-candle Mother-Bar EQ variant (same mechanic, this chapter just doesn't use that name).
- **p.425** ("don't chase Daily Bias past 50%, look for a PD array beyond it instead") — already `equilibrium-as-decision-point.md`'s premium/discount-mode-plus-required-PD-array rule.
- **p.426 summary points 1–4** (combine with Weekly/Monthly, prior-week-high/low importance, sweep-then-rebalance-at-EQ, prior-day-sweep as a good signal) — recap of top-down-analysis.md/liquidity/equilibrium coverage.
- **Soft4FX** (p.426, a backtesting tool name) and the undated 2014–2015 GBP/EUR-crisis anecdote (p.427) — no mechanics; unlike `THAI-COMMUNITY-2026-SMART-MONEY-REVERSAL`'s Myfxbook mention (kept because it verified a documented technique), neither adds anything actionable.

Updated `SOURCES.md` (new entry, noting this is the first contiguous page range in the run) and `TIMELINE.md` (2026 Year-Refined bullet only). No `INDEX.md` or `GLOSSARY.md` changes, no new file, `weekly-bias.md` deliberately untouched. Ran a background lint pass (JSON validity, Source-ID top-matter/JSON mirroring, `related[]` mirroring, cross-links resolve, repo-wide citation grep, cross-file ordinal consistency between `SOURCES.md`/`TIMELINE.md`) before presenting for review.

## [2026-08-10] ingest | Thai Community chapter — Session Killzone

Ingested "ICT Session Kill Zone.pdf" (21 pages: 1 unnumbered cover + pp.436–455; 53MB, read in two calls, pages 1–20 then page 21). New Source ID `THAI-COMMUNITY-2026-SESSION-KILLZONE` — a twelfth ingested chapter. Page numbering is *not* contiguous with the immediately-prior ingest (`THAI-COMMUNITY-2026-DAILY-BIAS-HOW-TO-USE`, ending p.427) — pp.428–435 are unaccounted for, alongside p.369; neither gap guessed at.

A foundational Session/Killzone overview chapter — mostly recap of thoroughly-documented ground. Surveyed against `concepts/15-sessions/` (all 9 files), `concepts/10-killzones/` (all 8 files), `concepts/23-ipda/` (all 7 files), `true-day-open.md`, `standard-deviation-projections.md`, and grepped repo-wide for "CBDR"/"Central Bank Dealer" (zero hits) and "Time + Price = Money" (zero hits) before scoping. `advisor()` separated two real gaps from two false leads: CBDR (write it — completely undocumented, real ICT terminology, definition + criteria + application is enough to fill the template) and IPDA Times (write a short addition, but flag the source's own table/prose rendering conflict rather than picking one number) are genuine; the NY AM killzone time discrepancy (source says 07:00–10:00 vs. wiki's canonical 08:00–11:00) doesn't qualify as a citable correction because the *same source* labels that identical window "Killzone" on p.447 and "Session" on p.448 — internally inconsistent about what kind of thing the number labels, so it can't override three files (`killzone-overview.md`, `killzone-times-table.md`, `ny-am-killzone.md`) that all agree on 08:00–11:00 with three nested sub-windows that only make sense inside that anchor.

**New file:** `concepts/04-time-cycles/central-bank-dealing-range.md` — CBDR, 14:00–20:00 NY, expected sideways/consolidating behavior, used qualitatively as a next-day price-target projection base (source gives no specific multiplier — not invented here). `ICT Confidence: high` (real ICT terminology), `Year Introduced: 2016` — dated by association with the same `ICT-2016-KILLZONES` table family CBDR is presented alongside in this source, not from an explicit date the source itself gives; flagged as such in both this file and the timeline entry. Checked `standard-deviation-projections.md` first for whether it already owns a CBDR-adjacent projection formula — it doesn't (no CBDR mention at all), and the source itself gives no multiplier to reproduce, so no Formula/Math beyond the qualitative statement. Cross-linked from `killzone-times-table.md` as an adjacent reference window on the same NY-clock table, explicitly distinguished from that file's canonical 5-killzone list (which uses NY PM in the same time slot CBDR partially overlaps) — not merged into it.

**Enrichment:** `ipda-definition.md`'s Formal Criteria gained "IPDA Times" — a claimed 00:00→15:00 NY daily algorithm-active window, distinct from the existing 20/40/60-day lookback tiers. The source's own summary table renders the close as "03:00" while its prose says "closes 3pm" and "15 hours per day" in the same passage; since 00:00→15:00 is exactly 15 hours, the table figure is treated as a rendering collision (likely a 15:00/3pm mis-transcription), not a second claimed window — the 15:00 close is what's recorded, with the ambiguity noted rather than silently resolved.

**Explicitly out of scope, checked against existing coverage before excluding:**
- **pp.436–439** — crowd-delusion/professional-trader-lifestyle motivational essay and a bank-ATM-secrecy analogy for why institutional timing must stay opaque. No mechanics.
- **Currency pairs active per session** (AUD/NZD/JPY-Asia p.442, CHF/EUR/GBP-London p.444, USD/CAD-NY p.446) — general FX market structure, not ICT methodology; out of AGENTS.md's definitional-library scope.
- **"Time + Price = Money"** (p.454, attributed to ICT's own YouTube teaching) and **Turtle-Soup-underlies-every-killzone** (pp.452–453) — both already implied by existing `killzone-overview.md` ("setups belong in killzones"), `pd-array-confluence.md` (time-of-day as a confluence factor), and the four `13-judas-swing/` files' existing killzone linkage. No new criterion either adds.
- **p.453** — the author's own STARTUPFOREX (2018–2020)/Volume-observation autobiographical aside, which the author himself explicitly excludes from this book's teaching content; and the Quasimodo/RTM comparison, same out-of-scope ruling already applied to `THAI-COMMUNITY-2026-TURTLE-SOUP`'s prior Quasimodo mention.
- **p.455** — the four-point "how to use Killzone" checklist — already `fvg-setup-checklist.md` + `killzone-overview.md` + `equilibrium-as-decision-point.md` instantiated.

Updated `SOURCES.md` (new entry), `TIMELINE.md` (a 2016-section note for the new file plus a 2026 Year-Refined/new-file bullet, matching the MMXM shape), `INDEX.md` (new-file entry under 04 — Time Cycles, after `dst-handling`), and `GLOSSARY.md` (new CBDR entry — caught on self-review that CBDR is exactly the abbreviation-lookup case GLOSSARY.md exists for, initially missed it). Ran a background lint pass (JSON validity and section-order for the new file, Source-ID top-matter/JSON mirroring across all 3 touched files, `related[]` JSON/prose mirroring, INDEX.md entry presence, repo-wide citation grep, cross-file ordinal consistency check between `SOURCES.md`/`TIMELINE.md`) before presenting for review.

## [2026-08-10] ingest | Thai Community chapter — CBDR

Ingested "ICT CBDR.pdf" (12 pages: 1 unnumbered cover + pp.457–467; read directly in a single call). New Source ID `THAI-COMMUNITY-2026-CBDR` — a thirteenth ingested chapter, and the first page range in this run **contiguous** with the immediately-prior ingest (`THAI-COMMUNITY-2026-SESSION-KILLZONE`, ending p.455). pp.428–435 and p.369 remain the only unaccounted-for pages in the book.

A dedicated deep-dive on CBDR, the concept the prior ingest introduced skeletally — that entry's own text said the source "gives no specific multiplier levels"; this chapter supplies exactly that, retiring the earlier hedge. Surveyed `london-open-killzone.md` (reverse/wick/tail language), `macro-times-overview.md` and `true-day-open.md` ("30 min"/"true open" language), and read `standard-deviation-projections.md` in full to confirm its existing SD system is swing-leg-anchored (a different mechanism from the PDF's CBDR-anchored STD table) before scoping. `advisor()` separated four real items from two traps: (1) the CBDR STD table/validity band — write it, this is the main event, drop the "no multiplier" hedge and say plainly it's now false; (2) the NY Killzone 07:00–10:00 restatement (p.463, same table, same author) — still exclude, a cleaner restatement of the same claim by the same source isn't independent corroboration, even though this printing resolves the earlier internal-inconsistency objection; (3) IPDA Times — update the hedge to reflect resolution, don't delete the trail; (4) "Open True Day per killzone" — blocks unless renamed, since the source's own label collides with `true-day-open.md`'s existing term; keep only the London instance (stands on its own under the wiki's canonical 02:00 anchor) as a functional timing observation, skip the NY instance (only makes sense under the 07:00 NY-killzone anchor just declined above) and the label itself.

**Substantially rewrote `central-bank-dealing-range.md` (no new file):**
- Formal Criteria + Formula/Math gained: a 200–400 pip CBDR-range validity band with an Asian Range (20:00–00:00 NY) fallback above ~400 pips (p.457, p.462); the CBDR-anchored 1–4 STD projection system — integer multiples of the CBDR range itself, symmetric above (Sell Day/HOD target) and below (Buy Day/LOD target), with the day's actual extreme normally landing at 1–2 STD and 3–4 STD read as a news-expansion signal rather than the base case (pp.458–461).
- Explicitly disambiguated, both directions, from `standard-deviation-projections.md`'s pre-existing swing-leg-anchored negative-fib SD system (−1.5/−2.0/−2.5/−4.0) — same "SD"/"STD" abbreviation, different anchor mechanism; cross-linked in both files' Related Concepts/Citations, not merged. `standard-deviation-projections.md` gained the reciprocal note, a new Source ID, and a Citations line.
- Retired the two now-false hedges in Common Mistakes and Formula/Math ("source gives no formula," "don't invent one") — both replaced with the actual rule.
- Common Mistakes gained: conflating the two STD systems, projecting from a CBDR range outside the validity band, and treating a 3–4 STD print as the base case instead of the exception.
- Examples: replaced the old 25-pip placeholder (now inconsistent with the 200–400 pip validity band) with one worked example inside the band, noting the source itself gives two (bearish p.460, bullish p.461) sharing one structure — kept one, cited both pages.

**Two smaller enrichments:**
- `ipda-definition.md`'s IPDA Times bullet — downgraded from "flagged, not fully resolved" (an earlier chapter's table/prose rendering mismatch on the close time) to a resolved note: this chapter restates the window as a clean 00:00–15:00 NY IPDA Reset with no mismatch (p.463), resolving the render collision in favor of 15:00. New Source ID and Citations line added; the resolution trail is kept, not deleted.
- `london-open-killzone.md` gained one clause on its existing sweep-then-displace sentence (the swept extreme typically becomes the day's eventual wick once price reverses away from it) and one new Formal Criteria bullet (true directional expansion often lags the killzone's nominal 02:00 open by ~30 minutes, landing around 02:30 — the source's own "Open True Day" label is not adopted, since it collides with `true-day-open.md`'s existing 00:00 NY daily-anchor term; only the timing observation was kept, the name was dropped, with the collision spelled out in-file). `Year Refined` bumped 2022→2026; new Source ID and Citations line added.

**Explicitly out of scope, checked against existing coverage before excluding:**
- **p.463, the source's second "New York Killzone 07:00–10:00" statement** — same table reused by the same author as the prior chapter's p.447 statement; not independent corroboration, so the independence objection from that earlier ruling stands even though this printing doesn't also mislabel the window "Session" elsewhere (resolving only the internal-inconsistency objection, not the independence one).
- **pp.463–465, the full killzone→MT4/MT5 broker-time conversion table** — a fixed-offset convenience table; `dst-handling.md` already covers MT4/MT5 server-time re-anchoring generically without committing to any one broker's offset, so this table adds no new criterion.
- **p.457, two of the "3 events learned from London Killzone"** (creates the day's H/L, then reverses) — already `london-open-killzone.md`'s existing sweep→displace-opposite→toward-DOL sentence; only the third (becomes the day's wick) was genuinely additive, folded in as one clause.
- **p.466, the practical killzone-entry checklist** — recap, the same `fvg-setup-checklist.md` + `killzone-overview.md` + `equilibrium-as-decision-point.md` ground the prior chapter's checklist was already ruled out-of-scope against.

Updated `SOURCES.md` (new entry, noting the contiguous page range) and `TIMELINE.md` (2026 Year-Refined bullet only — no new file, no Community-Attributed entry). No `INDEX.md` change (file already listed) and no `GLOSSARY.md` change (CBDR entry already added last ingest). Ran a background lint pass (JSON validity, Source-ID top-matter/JSON mirroring across all 4 touched files, `related[]` JSON/prose mirroring on both directions of the new cross-link, cross-links resolve, repo-wide citation grep, cross-file ordinal consistency check for "thirteenth" between `SOURCES.md`/`TIMELINE.md`) before presenting for review.

## [2026-08-11] ingest | Thai Community chapter — Asian Range

Ingested "ICT ASIAN RANGE.pdf" (5 pages: 1 unnumbered cover + pp.469–472; read directly in a single call). New Source ID `THAI-COMMUNITY-2026-ASIAN-RANGE` — a fourteenth ingested chapter, contiguous with the immediately-prior ingest (`THAI-COMMUNITY-2026-CBDR`, ending p.467) — the third contiguous page-range pair in this run and the second consecutive one. pp.428–435 and p.369 remain the only unaccounted-for pages in the book.

A short chapter, mostly recap of `asian-range.md`/`asian-range-sweep.md`/`asian-session-bias.md`'s existing coverage. Surveyed those three files plus `pd-array-confluence.md` and `standard-deviation-projections.md` before scoping. `advisor()` caught something more important than any of the four candidates I'd queued: re-reading this chapter's own "200-400 จุด" Asian-range-size figure against the CBDR chapter's identical wording for its validity band exposed that **the immediately-prior ingest (commit `c8b6f9d`) had gotten a real fact wrong** — I had rendered the CBDR chapter's own "200-400 จุด" as "200-400 pip validity band (major FX pairs)" in `central-bank-dealing-range.md`. Neither the unit ("จุด" is MT4/MT5 broker-point terminology, not pips — 200-400 pips on EURUSD in a 6-hour CBDR window isn't physically plausible) nor the instrument scope ("major FX pairs") was stated by that source; both were things I added while writing, not things I read. Re-reading the CBDR chapter's own worked-example pages (pp.460-461) to check confirmed the charts use abstract STD-unit axes with no raw price levels — they couldn't have told me the unit either time; the actual tell was rereading p.457's plain prose, which names no instrument at all.

**Corrected `central-bank-dealing-range.md` (not a new file, a fix to an existing entry, with a trail):**
- Formal Criteria, Formula/Math, Common Mistakes, Related Concepts, and Citations all changed "pip" → "point," and the invented "(major FX pairs)" instrument scope was removed and explicitly flagged as wrongly attributed in an earlier version of the file.
- Added the pip-conversion note (10 points = 1 pip on 5-digit FX feeds, so 200-400 points ≈ 20-40 pips) and the observation that this reading is now consistent with this wiki's own pre-existing Asian-range size figure (25-60 pips EURUSD) rather than contradicting it.
- The worked Example was already stated in points (US30, 250 points) and needed no numeric change — only the surrounding "pip"/"FX pairs" language around it was wrong, not the number itself.
- Added a citation crediting `THAI-COMMUNITY-2026-ASIAN-RANGE` for surfacing the error, and linked the CBDR>400-point fallback to `asian-range-projections.md`'s existing extension-target system (it previously pointed at `asian-range.md` generically with no system named).

**Two small, genuinely new additions:**
- **`asian-range.md`** — a "breakout-and-retest continuation" note (p.470, the chapter's own cover-subtitle thesis: "building a foundation for Breakout"): distinct from the wick-based Judas sweep already on `asian-range-sweep.md`, this is a bound the range price actually *closes through* with displacement, which then gets retested from the far side (breaker/flip-zone mechanic) before continuing. The file's existing Common Mistakes rule ("the range is not a pivot") was scoped to the *unbroken* range rather than rewritten, so the new note doesn't read as contradicting it.
- **`asian-range-projections.md`** — the file's own pre-existing confidence hedge ("specific multiples vary across teachings... some references use fib ratios") gained a third citable instance: this chapter's diagram labels Asian-range-anchored levels with `standard-deviation-projections.md`'s negative-fib-style −1/−2/−2.5 notation rather than this file's own 0.5×/1×/1.5×/2× convention. The diagram doesn't make legible which bound the levels are measured from, so the anchor bound is explicitly **not asserted** — recorded as a notation variant only, same discipline as leaving `weekly-bias.md` untouched in an earlier ingest rather than guessing a threshold.

**Explicitly out of scope, checked against existing coverage before excluding:**
- **p.472, two confluence chart examples** (a Demand Zone and a Fair Value Gap, each shown overlapping the Asian range) — illustrative only, no stated threshold or rule; this is `pd-array-confluence.md`'s existing factor 1 (spatial nesting), not a new confluence factor, same category as chart walkthroughs already excluded in earlier ingests.
- **"Open True Day" (p.471 diagram)** — a third sighting of this label in the same position relative to London KZ/NY KZ as the CBDR chapter's diagram. No file change; noted here only as confirming, not changing, the CBDR ingest's non-adoption decision (the label still collides with `true-day-open.md`'s daily-anchor term).

Updated `SOURCES.md` (new entry, plus a correction to the `THAI-COMMUNITY-2026-CBDR` entry's own text, which had repeated the same "pip" error) and `TIMELINE.md` (2026 Year-Refined bullet, plus a correction note on the prior bullet). No `INDEX.md` or `GLOSSARY.md` changes — no new file, no new abbreviation ("AR" is already an existing alias). Ran a background lint pass (JSON validity, Source-ID top-matter/JSON mirroring across all 3 touched files, `related[]` mirroring on all new cross-links, cross-links resolve, repo-wide citation grep, cross-file ordinal consistency check for "fourteenth" between `SOURCES.md`/`TIMELINE.md`) before presenting for review.

## [2026-08-11] ingest | Thai Community chapter — London Killzone

Ingested "ICT LONDON Kill Zone.pdf" (8 pages: 1 unnumbered cover + pp.474–480; read directly, split into three calls for closer reading — pp.1-3, 4-6, 7-8). New Source ID `THAI-COMMUNITY-2026-LONDON-KILLZONE` — a fifteenth ingested chapter, contiguous with the immediately-prior ingest (`THAI-COMMUNITY-2026-ASIAN-RANGE`, ending p.472) — the third *consecutive* contiguous page-range pair in this run. pp.428–435 and p.369 remain the only unaccounted-for pages.

**Zero wiki changes this ingest.** Surveyed `london-open-killzone.md`, `turtle-soup.md` + the rest of `20-turtle-soup/`, `judas-swing.md`, `top-down-analysis.md`, `mss-vs-choch.md`, `cisd.md`, `draw-on-liquidity.md`, `internal-range-liquidity.md`, and grepped repo-wide for "inducement" before scoping. `advisor()` confirmed the read was right rather than second-guessing it, and sharpened two of the four candidates I'd queued:

1. **The chapter's core content is Turtle Soup timing applied to the London killzone** (02:00–05:00 NY) — already `london-open-killzone.md`'s entire subject, already `turtle-soup.md`'s definition, no new mechanic in the pairing itself.
2. **p.474's stated precondition** ("even if a Turtle Soup pattern forms, price may not reverse unless it first swept a real liquidity pool") looked like it might be an under-documented invalidation condition — the pattern this run has repeatedly found genuinely new (the MMBM invalidation gate, the weekly check-stop). Checked directly against `turtle-soup.md`: its own Formal Criteria already requires the wick to breach a "**known** SSL/BSL level" (not just any wick) for the pattern to qualify at all, and its Common Mistakes section already states "**Calling every wick a Turtle Soup.** The level must be known liquidity... random wicks don't qualify." Both cover this precondition exactly. Not a gap.
3. **p.475's "Internal SSL (inducement)" diagram label** — the one candidate that looked genuinely new on first pass, since "inducement" appears in the wiki only once, as a bare unelaborated mention (`mss-vs-choch.md:113`, "Level 3: CHoCH + IDM (Inducement) + BOS + Sweep"). `advisor()`'s sharper framing: that existing mention already places IDM/Inducement in the **community-SMC** column and states the strict-ICT ladder discards it entirely (no IDM step). Elaborating the term from a single incidental diagram label with zero accompanying prose would be adding definitional weight to a term this wiki has deliberately parked — the same shape as declining to adopt the NY-07:00-killzone restatement two ingests ago as "not independent corroboration." Excluded, and logged with that specific reason rather than "too thin" alone.
4. **p.476's Power-of-Three/AMD diagram** — the author's own text explicitly defers this to a later Power of Three chapter ("I've written more on this in the Power of Three section... you can study further in upcoming chapters"). Same self-deferral precedent as the STARTUPFOREX aside several ingests back. Also a forward pointer: a dedicated Power of Three chapter is presumably still ahead in the book, worth watching for.
5. **pp.477–480's worked example** ("Sell Day Setup," 3 parts — Bias, Liquidity Pools, Result) — CISD + FVG + MSS + Displacement entry, stoploss above the CISD order block, TP at Asian Low / prior-day-low. Full recap of `cisd.md` + `mss-vs-choch.md` + `draw-on-liquidity.md`; the HTF-POI-by-entry-TF table on p.474 is the same skip-a-rung pairing `top-down-analysis.md` already documents (restated a second time in this run, having also appeared in an earlier ingest).

**"Open True Day / Midnight Open" — a fourth sighting**, across the pp.476–480 diagrams. Worth recording precisely because this instance explicitly pairs the label with "Midnight Open" (00:00 NY) rather than the CBDR chapter's ~30-minutes-after-nominal-killzone-open framing — a small piece of evidence that the CBDR chapter's usage, not this one, was the outlier. No file change; noted here as slightly firming the existing non-adoption decision, not reopening it.

Updated `SOURCES.md` only (new entry with the full exclusion audit trail — the deliverable for a zero-edit ingest). No `TIMELINE.md` entry (no concept file's `Year Refined` changed — nothing to bump), no `INDEX.md`/`GLOSSARY.md` changes, no new file. Ran the ordinal-consistency check ("fifteenth" appears correctly in `SOURCES.md`; no `TIMELINE.md` entry expected this time, confirmed deliberately rather than a lint gap) before presenting for review.

## [2026-08-11] ingest | Thai Community chapter — New York Killzone

Ingested "ICT NEW YORK Kill Zone.pdf" (9 pages: 1 unnumbered cover + pp.482–489; read in three close-reading calls — pp.1-3, 4-6, 7-9). New Source ID `THAI-COMMUNITY-2026-NY-KILLZONE` — a sixteenth ingested chapter, contiguous with the immediately-prior ingest (`THAI-COMMUNITY-2026-LONDON-KILLZONE`, ending p.480) — the fourth consecutive contiguous page-range pair in this run, fifth overall (counting `-DAILY-BIAS-HOW-TO-USE`'s earlier, separate pairing).

Surveyed `ny-am-killzone.md`, `ny-pm-killzone.md`, `london-session.md`, `silver-bullet-ny-am.md`, `dst-handling.md`, `ict-fib-overview.md`, `symmetrical-price-projections.md`, `pd-array-confluence.md`, and `asian-range.md` before scoping. `advisor()` confirmed the overall read (near-total recap) but sharpened three things and caught one record-keeping error:

1. **The continuation-or-reversal clause needed a named referent.** My first draft would have added an unanchored "continuation or reversal" to `ny-am-killzone.md`. Both sibling files anchor it explicitly — `ny-pm-killzone.md` to "the AM trend," `london-session.md` to "London open's move" — and the source itself names the referent (p.482: continuation of the trend "ที่เกิดขึ้นจาก London Session," i.e. what London delivered). Rewrote to match that pattern exactly, which also makes it non-redundant with the file's existing "typically where the daily HOD or LOD is established" sentence rather than in tension with it.
2. **The Midnight Open / MT4-MT5 "07:00" note isn't just genericaly covered — it's the same offset already worked.** Checked the arithmetic: 00:00 NY displaying as 07:00 on a chart requires a GMT+2 server (NY EST is GMT−5, +7h offset). `dst-handling.md`'s own "Example 2 — broker-time chart at GMT+2" already carries this exact offset on a killzone window. Third sighting of broker-clock trivia this run (after the CBDR chapter's MT4/MT5 killzone table); same ruling, cited to the specific existing example rather than "covered generically."
3. **The −27%/−62% fib labels — checked, not presumed, before excluding.** My first instinct was to reason they're "probably" part of the already-documented extension family. `advisor()` caught that as an inference, not a check. Opened `symmetrical-price-projections.md` (previously unread this run) and re-checked `ict-fib-overview.md`'s full level table directly: −0.27 and −0.62 appear in neither. Confirmed absent, but a bare diagram axis label with zero accompanying prose is too thin an evidentiary basis to add even as a citation-level footnote — unlike the Asian Range chapter's −1/−2/−2.5 STD notation, there's no sentence here to anchor a note to. Excluded, logged as "checked, confirmed absent, still too thin," not "presumably covered."
4. **A running-count error surfaced while reconciling the contiguity claim for this entry.** `SOURCES.md`'s `-CBDR` entry said "the first ingest this run whose page numbering is contiguous" — wrong; `-DAILY-BIAS-HOW-TO-USE` held that distinction several ingests earlier (its own entry says so correctly). Corrected the `-CBDR` entry's text with a trail, same precedent as its pip/point unit correction two ingests ago.

**One genuine, small addition:** `ny-am-killzone.md`'s Definition gained a continuation-or-reversal-of-London clause, `Year Refined` bumped 2022→2026. Not new content on its own terms — `ny-pm-killzone.md` and `london-session.md` already state the identical duality for their own windows — but a real completeness gap: this file was the one inconsistency in an otherwise-uniform sibling pattern, and the new source's own cover-subtitle thesis ("Continuation or Reversal" as NYKZ's two behavioral modes) is what surfaced it.

**Explicitly out of scope, checked against existing coverage before excluding:**
- **p.482, NYKZ definition and volume/USD-index framing** — recap of `ny-am-killzone.md`'s existing content.
- **p.483, pointer to Silver Bullet as the next topic** (timing given as "22:00–23:00 Thai time") — converts correctly to the existing 10:00–11:00 NY anchor; already `silver-bullet-ny-am.md`.
- **p.484, a 5-point "what to expect" checklist** (POI, higher-TF liquidity pools, Old High/Low retest, strong PD arrays, Asian/London session extremes) — already the combined ground of `pd-array-confluence.md` + `draw-on-liquidity.md` + `killzone-overview.md`; no new factor.
- **pp.485–486, a "Continuation" trading idea** — the author's own text defers this to a later Power of Three/AMD chapter, same self-deferral precedent as the London Killzone chapter's AMD diagram.
- **p.488, an Asian-Range breakout-and-retest worked example during NYKZ** — the identical mechanic added to `asian-range.md` two ingests ago, recap in a new session context.
- **p.489, a "Reversal" trading idea** via CISD + FVG + MSS + Displacement — recap of `cisd.md` + `mss-vs-choch.md`.

**"Open True Day" — a fifth sighting**, across pp.486, 487, 489, paired with "Midnight NY Open" on p.488. The p.487 usage ("Retest: Asian Range & Open True Day") is the sharpest evidence yet: it treats OTD as a *retested price level*, which only coheres under this wiki's existing 00:00-NY-anchored `true-day-open.md` reading (a level) — not under the CBDR chapter's "~30 minutes after nominal killzone open" reading (an instant in time, not a level that can be "retested"). This moves the CBDR chapter's usage from "outlier" toward "likely a misread of the same term," worth recording for the next chapter rather than re-litigating from scratch. No file change — logged only. Left the HOD/LOD-ownership hedge on both `london-open-killzone.md` and `ny-am-killzone.md` alone, per `advisor()`: neither this chapter nor prior ones adjudicate between them, and it's not this ingest's business to.

Updated `SOURCES.md` (new entry, plus the `-CBDR` contiguity-count correction), `TIMELINE.md` (2026 Year-Refined bullet). No `INDEX.md`/`GLOSSARY.md` changes, no new file. Ran the ordinal-consistency check ("sixteenth" in `SOURCES.md`, contiguity counts reconciled across all five affected entries) and the standard lint pass (JSON validity, Source-ID mirroring, `related[]` resolution, cross-link resolution) before presenting for review.

## [2026-08-12] ingest | Thai Community chapter — Silver Bullet

Ingested "ICT Silver Bullet.pdf" (10 pages: 1 unnumbered cover + pp.491–499). New Source ID `THAI-COMMUNITY-2026-SILVER-BULLET` — a seventeenth ingested chapter, contiguous with the immediately-prior ingest (`THAI-COMMUNITY-2026-NY-KILLZONE`, ending p.489) — the fifth consecutive contiguous page-range pair in this run, sixth overall.

Surveyed all five existing `11-silver-bullet/` files (`silver-bullet-overview.md`, `silver-bullet-rules.md`, `silver-bullet-ny-am.md`, `silver-bullet-london.md`, `silver-bullet-failure-modes.md`, `silver-bullet-formalized-2025.md`) before scoping, plus `external-range-liquidity.md`, `internal-range-liquidity.md`, `buy-side-liquidity.md`/`sell-side-liquidity.md` (for the DOL liquidity-type list), and `bearish-order-block.md`/`mss-vs-choch.md`/`cisd.md` (for the worked examples). `advisor()` caught the one thing that mattered before any file was touched:

1. **My first read of the p.491 Silver Bullet Times table (London 03:00–04:00 NY) was "this matches pre-2026-shift teaching, so it's recap."** That's wrong — every sibling Source ID in this run is `THAI-COMMUNITY-2026-*`, so this is a **2026** source stating 03:00–04:00, which directly conflicts with `silver-bullet-london.md`'s own `ICT-2026-LONDON-SB-SHIFT` claim that the window moved to 02:00–03:00 NY *in 2026*. Framing it as recap would have buried the highest-value finding in the chapter. `advisor()` flagged this and specified the discriminating check: confirm it isn't an OCR/translation artifact by cross-checking the table's own Thai-time column independently. It isn't — บ่าย 3–4 โมง = 15:00–16:00 Thai = 03:00–04:00 NY exactly (the other two rows check out clean too: 4–5 ทุ่ม = 22:00–23:00 Thai = 10:00–11:00 NY; ตี2–ตี3 = 02:00–03:00 Thai = 14:00–15:00 NY, both matching the wiki). Recorded as an unresolved **Time Conflict**, not a correction — the source never addresses the shift claim, so it may simply be republishing pre-2026 knowledge rather than contradicting it. Added a callout blockquote + Citations line to `silver-bullet-london.md` and a footnote to `silver-bullet-overview.md`'s windows table, following the `mitigation-block.md` disputed-term idiom — but scoped narrowly: this is a parametric (clock-time) dispute, not a definitional one, so whole-file confidence stayed `high` rather than flipping to `disputed` the way `mitigation-block.md` did for a genuinely incompatible two-reading conflict.

**One genuine, small addition:** `silver-bullet-overview.md` gained a new "Sideways-Week Behavior (community-attributed)" section — the source's own U-shape range observation for consolidating weeks (p.498): an SB-window price extreme on one day tends to reappear at the *same* SB window ~24 hours later (worked example: a Monday London-SB high, a Tuesday London-SB return to the same level), illustrated with two GBPJPY 15m charts (23–25 Jan 2024). No stated tolerance band or minimum-consolidation length, so recorded as a behavioral note to watch for, not a formal criterion — no new file, no criteria-table change.

**Explicitly out of scope, checked against existing coverage before excluding:**
- **p.491, the DOL liquidity-type list** (Old High/Low of prior day, prior week, prior session) — already `buy-side-liquidity.md`/`sell-side-liquidity.md`'s existing PDH/PWH/PDL/PWL + session-extreme enumeration.
- **p.491's "10-40 Pips" / "20-40 Pips" scalp-range figures** — a different quantity from the CBDR/Asian-Range chapters' "จุด"-unit range-size figure two ingests back (this chapter writes "Pips" in Latin script, not "จุด") — noted for the record since the earlier correction turned on exactly that distinction, but it does not reopen or retroactively settle that unit question.
- **p.493, the LuxAlgo "ICT Silver Bullet" TradingView indicator settings screenshot** — third-party tool configuration, same exclusion class as the MT4/MT5 broker-time conversion table two ingests back.
- **Examples 1–3 (pp.494–497)** — recap of `bearish-order-block.md` + `mss-vs-choch.md` + `cisd.md` (Examples 1 and 2, -OB/MSS/Displacement sequences) and `internal-range-liquidity.md`/`draw-on-liquidity.md` (Example 3, bearish FVG → rebalance → liquidity-target mechanic).
- **p.499's closing two-outcome summary** (run to External Range Liquidity, or run to Internal Range Liquidity for repricing then continue toward ERL) — already `external-range-liquidity.md`/`internal-range-liquidity.md`/`draw-on-liquidity.md`'s combined ground.

**Logged but not written to any concept file:**
- **p.499, the author's personal preference** for trading the full 3-hour Killzone over the 1-hour SB window, plus a recommendation for "the SB that occurs after New York Killzone" — the latter is ambiguous about which of the three windows it actually names, not clear enough to adjudicate against `silver-bullet-overview.md`'s existing Highest/Medium/Lowest probability-rank table. Left the table untouched, same precedent as leaving the HOD/LOD-ownership hedge alone two ingests back.
- **A sixth "Open True Day" sighting** (pp.494–495, again drawn as a retested horizontal level, paired with "Midnight Open") — further firms, does not upgrade, the prior ingest's "likely a misread" reading of the CBDR chapter's usage. Six sightings from the same author is still one author, per this run's own hedge-retraction-and-independence-check lesson — the ruling stays logged, not escalated.

**pp.492–493's prose** (a "why might 1 hour of SB not be enough, backtest across 2 windows" discussion) was substantially illegible on close reading — rotated, low-confidence OCR text — and was deliberately **not** paraphrased into any concept file. Its clearer restatement on p.499 (the Killzone-over-SB preference, logged above) was used instead of guessing at the garbled version, per the "checked, confirmed absent, still too thin" / do-not-speculate precedent from this run's earlier pp.369–400 mis-speculation.

Updated `silver-bullet-london.md` (Time Conflict callout, Source ID, Citations), `silver-bullet-overview.md` (windows-table footnote, new Sideways-Week Behavior section, Source ID, Citations), `SOURCES.md` (new entry), `TIMELINE.md` (2026 Year-Refined bullet). No `INDEX.md`/`GLOSSARY.md` changes, no new file. Ran the ordinal-consistency check ("seventeenth" in `SOURCES.md`, contiguity count reconciled from `SOURCES.md` directly rather than from memory) and the standard lint pass (JSON validity on both touched machine-readable blocks, Source-ID mirroring between frontmatter/Citations/JSON, cross-link resolution) before presenting for review.

## [2026-08-15] ingest | Thai Community chapter — Power of Three

Ingested "ICT Power Of Tree.pdf" [sic, filename typo for "Three"] (19 pages: 1 unnumbered cover + pp.501–518). New Source ID `THAI-COMMUNITY-2026-POWER-OF-THREE` — an eighteenth ingested chapter, contiguous with the immediately-prior ingest (`THAI-COMMUNITY-2026-SILVER-BULLET`, ending p.499) — the sixth consecutive contiguous page-range pair in this run, seventh overall. This is the dedicated PO3/AMD chapter that both the London Killzone and NY Killzone chapters explicitly deferred to a few ingests back.

Surveyed all 6 existing `12-power-of-three/` files (`power-of-three.md`, `accumulation-phase.md`, `manipulation-phase.md`, `distribution-phase.md`, `htf-amd.md`, `intraday-amd.md`) plus `24-amd-cycle/amd-cycle-overview.md`/`amd-vs-po3.md`, `daily-bias.md`, `true-day-open.md`, `external-range-liquidity.md`, `internal-range-liquidity.md` before scoping. `advisor()` caught the finding that mattered before any file was touched:

**The OHLC/OLHC decode.** `daily-bias.md` already cites "OLHC"/"OHLC" as labels the source's own earlier chapter (`THAI-COMMUNITY-2026-DAILY-BIAS-CLOSE`, pp.406–407) gives the bullish/bearish next-day-bias branches — cited there without being defined, and a GLOSSARY entry was explicitly ruled out at the time as misleading (since "OHLC" doubles as universal chart-data notation). My first read of this chapter's own "OHLC-OHLC" / "OLHC-OHLC (กลับด้าน — reversed)" diagrams (pp.511, 514) treated them as a possible second, colliding sense of the same abbreviation — heading toward an SD/STD-style two-direction disambiguation. `advisor()` flagged the more likely reading: decode the letters as intra-candle path order (which extreme prints first) and check whether it's consistent with the *existing* labels rather than assuming collision. It is: bullish PO3 = Open→Low(manipulation sweep)→High(distribution)→Close = **OLHC**; bearish = the mirror, **OHLC** — which lines up exactly with `daily-bias.md`'s close-above-PDH="OLHC"/close-below-PDL="OHLC" mapping, and the chapter's own "กลับด้าน" (reversed) framing on p.512 supports OLHC being OHLC mirrored, not a different concept. This is same-author-explains-own-earlier-shorthand, not the "same evidence printed twice" independence problem this run's own lesson warns about — a different failure mode than the pattern it superficially resembles. `daily-bias.md` gained one explanatory footnote (not a rewrite); `power-of-three.md` gained the mechanic itself, since that's the genuinely new claim (manipulation prints the candle's first extreme, distribution the second) — a new lens on an already-documented mechanic (`manipulation-phase.md`'s "typically wicks the level then reverses"), not a new file.

**Two smaller genuine additions.** (1) `power-of-three.md` gained explicit ERL/IRL routing cross-links (pp.509–510: Manipulation sweeps External Range Liquidity at Old Highs/Lows, price rebalances into an Internal Range Liquidity PD array near equilibrium, Distribution runs toward the next ERL) — a real completeness gap, since no file in `12-power-of-three/` had previously linked either concept despite both already existing generically in `02-liquidity/`. (2) `amd-vs-po3.md` gained a "Power of Four" naming-variant section (p.503) — the source names a second 4-phase framing (splitting Distribution into Expansion + Distribution) distinct from this wiki's existing AMD-X (which appends a 4th phase after Distribution, not subdivides it) — the source calls its own naming choice unresolved, recorded as such rather than adjudicated, and distinguished from AMD-X via a new Common Mistakes bullet so the two 4-phase framings aren't conflated.

**Confirmed already covered, no action:** the 3-phase definition and formal criteria; bullish/bearish PO3 diagrams (pp.504–506, 515); session→phase mapping (Asia=Accumulation, London=Manipulation, NY=Distribution, p.516) — already `power-of-three.md`'s own Visual Pattern and `intraday-amd.md`; weekly-candle/4h fractal nesting (pp.507–508) — already `htf-amd.md`; the Bias-for-Bullish/Bearish worked example (p.513) and the 3-timezone Open-True-Day framing (p.509) — already `daily-bias.md` + `true-day-open.md`'s combined ground; the p.517 real-price fractal-nesting chart examples — illustrative, same category as prior chapters' worked-example exclusions; the concluding summary (p.518) — recap of the file's own opening definition.

**OCR caveat.** pp.509, 512, 513, and 518 were substantially rotated/low-confidence scans. The OHLC/OLHC reading rests on the pp.511/514 diagrams plus the `daily-bias.md` cross-check for decode-consistency, not on the garbled prose of those pages — none of which was paraphrased into any concept file.

Updated `power-of-three.md` (OHLC/OLHC mechanic, ERL/IRL cross-links, Source ID, Citations), `amd-vs-po3.md` (Power of Four section, new Common Mistakes bullet, Source ID, Year Refined 2022→2026, Citations), `daily-bias.md` (explanatory footnote on existing OLHC/OHLC labels, Source ID, Citations, cross-link), `SOURCES.md` (new entry), `TIMELINE.md` (2026 Year-Refined bullet). No `INDEX.md`/`GLOSSARY.md` changes, no new file. Ran `bin/lint-concepts.py` (the checked-in script from `3e7167e`, replacing the fresh-inline-script pattern of every prior ingest) — it correctly flagged the new Source ID as unregistered against `SOURCES.md` before that entry was written, then passed clean after.

## [2026-08-15] ingest | Thai Community chapter — Standard Deviation and Projection

Ingested "ICT STD Projection.pdf" (39 pages: 1 unnumbered cover + pp.520–557). New Source ID `THAI-COMMUNITY-2026-STD-PROJECTION` — a nineteenth ingested chapter, contiguous with the immediately-prior ingest (`THAI-COMMUNITY-2026-POWER-OF-THREE`, ending p.518) — the seventh consecutive contiguous page-range pair in this run, eighth overall.

Surveyed `standard-deviation-projections.md`, `central-bank-dealing-range.md`, `asian-range-projections.md`, `power-of-three.md` + its five sibling files, `weekly-quarters.md`, `daily-bias.md` before scoping. `advisor()` caught the finding that mattered before any file was touched.

**The routing call.** The chapter's own opening (p.521) names five "STDv" types. My first pass grouped all of them loosely under "PO3 + STD," heading toward filing the 2.0–2.5/4.0–4.5 pair under `power-of-three.md`. `advisor()` flagged that this specific pair is anchored to a **swing leg** (Manipulation-phase LH/HL → HTF POI), per the source's own p.544 measurement instruction — the same family as `standard-deviation-projections.md`'s existing −1.5/−2.0/−2.5/−4.0 set, not the CBDR/Asian/Flout time-bounded-range family. Filing it under PO3 alone would have undercut that file's own CBDR-disambiguation note rather than reinforced it. Routed correctly: `standard-deviation-projections.md` gets the definition, `power-of-three.md` gets a cross-link. The chapter turned out to be the strongest evidence yet for the existing disambiguation — same author, same word "STDv," two different anchors in the same chapter.

**Two new files, breaking the six-ingest enrichment-only streak.** (1) `flout-range.md` — a third STDv type: a combined 14:00–00:00 NY window (union of CBDR's window and the Asian killzone window), projected with CBDR's own 1–4× symmetric STD mechanic. The window was resolved from garbled OCR by triangulating two independent readings that agreed — the source's own English "2:00 PM – 12:00 AM" and its Thai gloss "ตี2 – เที่ยงวัน" (2am–noon Thai, converts to 14:00–00:00 NY) — then confirmed against the chapter's own diagram (p.531), where the "Flout Range" box visibly encompasses both "CBDR Range" and "Asian Range" sub-boxes rather than sitting beside them. (2) `tgif-weekly-po3.md` — the fourth STDv type: a Friday-specific weekly PO3 setup (Thursday BSL/SSL sweep lining up with a weekly/daily HTF FVG, retraced into to 20–30%, 50% a hard ceiling, typically during London Killzone). Confidence set `community-attributed` rather than `high` per the source's own explicit disclaimer (p.521): "ส่วนใหญ่จากการ Back Test ค่อนข้างแม่นยำ แต่ผมยังไม่เคยใช้จริงนะ" — backtested, never traded live. Cross-linked from `weekly-quarters.md`'s Friday row, which previously carried only an unelaborated "profit-taking / week's structure often resolves" placeholder.

**Three enrichments plus one decode upgrade.** `central-bank-dealing-range.md` gained the source's own name for the >4-STD regime this file already flagged as "news-expansion": **"Crisis Standard Deviation"** (a worked example reaching 6.5 STD, p.531) — a name attached to an existing rule, not a new rule. `asian-range-projections.md` gained a **fourth** documented multiple-notation variant ("Asia STD," pp.522–525: CBDR-style symmetric 1–4× of the full Asian range) — I read the file directly rather than trusting the prior ingest's SOURCES.md summary before concluding this strengthens, not retires, its existing "multiples vary" hedge; it does. `power-of-three.md`'s OHLC/OLHC candle-path decode (added last ingest, inferred from diagrams plus a cross-check against `daily-bias.md`) is upgraded to source-stated: this chapter spells both orders out in words on its own diagrams — "PO3 Open Low High Close" (bullish), "PO3 Open High Low Close" (bearish) — p.554. `daily-bias.md`'s explanatory footnote was updated to note the upgrade, not rewritten, per this run's own add-forward-don't-rewrite pattern.

**Confirmed already covered, no action:** Asia STD and CBDR STD (types 1–2 of the five) are pure recap of `central-bank-dealing-range.md`'s existing 1–4 STD mechanic applied to a sibling anchor; the chapter's own bullish/bearish PO3 + STD diagrams (pp.546–553) recap existing PO3-phase content with no new criterion beyond the routed 2.0–2.5/4.0–4.5 pair.

**OCR caveat.** Substantial rotation/low-confidence scanning on pp.530, 534, 539, and 544 meant the connective prose around several sections' core mechanics was not paraphrased — only the clearly legible mechanic in each case was kept, per this run's established "checked, confirmed absent, still too thin" precedent.

Updated `flout-range.md` (new), `tgif-weekly-po3.md` (new), `standard-deviation-projections.md` (2.0–2.5/4.0–4.5 pair, Source ID, Citations), `central-bank-dealing-range.md` ("Crisis Standard Deviation" naming, Source ID, Citations), `asian-range-projections.md` (fourth variant, Source ID, Citations), `power-of-three.md` (decode upgrade, cross-links, Source ID, Citations), `weekly-quarters.md` (TGIF cross-link, Source ID, Citations), `daily-bias.md` (footnote update, Source ID). `SOURCES.md` (new entry), `TIMELINE.md` (2026 Year-Introduced/Refined bullet), `INDEX.md` (two new entries), `GLOSSARY.md` (Flout, STDv, TGIF entries — this run's first GLOSSARY touch since the DAILY-BIAS-CLOSE chapter explicitly ruled OHLC/OLHC out of GLOSSARY scope). Ran `bin/lint-concepts.py` clean (240 files, 0 issues) after fixing one broken `related[]` reference it caught (`fair-value-gap-basics` → `fair-value-gap`).

## [2026-08-15] ingest | Thai Community chapter — Daily Profile & Weekly Profile

Ingested "ICT Daily Weekly Profile.pdf" (14 pages: 1 unnumbered cover + pp.559–571). New Source ID `THAI-COMMUNITY-2026-DAILY-WEEKLY-PROFILE` — a twentieth ingested chapter. **Not contiguous** with the immediately-prior ingest (`THAI-COMMUNITY-2026-STD-PROJECTION`, ending p.557) — p.558 is unaccounted-for, ending the seven-ingest consecutive-contiguity streak and joining pp.428–435 and p.369 on the book's gap list. Surveyed `weekly-quarters.md`, `weekly-bias.md`, `judas-swing.md`, `true-week-open.md`, `true-day-open.md`, `daily-bias.md`, `tgif-weekly-po3.md`, and `news-driven-overview.md` before scoping. `advisor()` caught the finding that mattered before any file was touched.

**The structural call.** pp.560–565 (daily Mon–Fri behavior narrative) turned out to be ~90% recap of `weekly-quarters.md`'s existing Mon-Tue-Wed-Thu AMD-X table. The real content is pp.566–571: six separate "ICT Weekly Profile" pages, each a Bullish/Bearish diagram pair naming a distinct weekly-candle shape. My first instinct, working page-by-page, was heading toward six small stub files. `advisor()` flagged this as fragmentation the run has avoided — `flout-range.md` and `tgif-weekly-po3.md` earned their own files because each was a genuinely distinct mechanic, but these six are one classification system. Wrote **one** new file, `weekly-profile-patterns.md`, hooked directly to the sentence already waiting for it: `weekly-quarters.md`'s own Common Mistakes line, "~50% of weeks follow it" — this taxonomy is what the other half look like. `weekly-bias.md`'s existing "Tuesday lows on bullish weeks" adage turned out, on rereading that file directly, to already be this taxonomy's Pattern #1 (Classic Tuesday Low/High of Week) — cross-linked rather than restated.

**The 20–30% arithmetic, checked not merged.** `tgif-weekly-po3.md` already carries a "0.20–0.30" figure (retracement *into an FVG* from a swept extreme). p.564 states a different-looking but same-number claim: a week that travels 2500 points typically retraces 500 on Friday — 500/2500 = 20%, the low end of this chapter's own stated 20–30% band. Confirmed these are two different measurements (retracement-into-an-FVG vs. retracement-of-the-week's-full-travel) that happen to share a number, same shape as the SD/STD and STDv disambiguations already made twice this run — kept separate, not collapsed, and `tgif-weekly-po3.md`'s own numbers/confidence were left untouched per the same-author independence lesson.

**The Monday/Friday tension, reconciled in text.** This chapter states plainly that Monday and Friday shouldn't be traded (pp.564–565) — but `tgif-weekly-po3.md`, an earlier chapter by the same author, presents a specific Friday-only setup. Not a contradiction: a general caution against trend-trading a quiet day versus one narrow carved-out exception inside that same day. Added a one-line reconciliation note to both `weekly-quarters.md`'s Friday elaboration and `tgif-weekly-po3.md`'s Common Mistakes, cross-linked both ways, no whole-file confidence change on either.

**The 23:00-Thai-time Thursday rule, stated as a band.** pp.563 and 565 both instruct closing profitable positions before "5 ทุ่ม" (23:00 Thai, GMT+7) on Thursday, since price is often already at the week's extreme past that point. Unlike the Silver Bullet chapter's time-conflict finding, there's no second time column here to cross-check the exact NY-clock hour against, so recorded as a **band** — 11:00–12:00 NY depending on DST — rather than pinning one hour. Added to `weekly-quarters.md`'s Thursday (Q4/X) row.

**Two genuine, small `true-week-open.md` additions.** p.562: a coarser rule comparing Wednesday's session open to the week's Monday open for continuation bias. p.565 (Note 1): a sharper, TDO-specific version — Wednesday's [true-day-open](concepts/22-quarterly-theory/true-day-open.md) (00:00 NY) should sit above the True Week Open for a buy bias to hold. Neither was previously carried by that file; both are genuine TWO/TDO interaction rules, not restatements.

**Explicitly out of scope, checked against existing coverage before excluding:**
- **p.559's fundamentals-avoidance framing** (GDP, interest rates, high-impact news pre-priced before retail can act) — recap of `news-driven-overview.md`'s existing "algorithmic delivery accelerators, not exogenous shocks" framing.
- **p.562's post-Tuesday-reversal Equilibrium-target note** ("if price reverses Tuesday, target last week's Equilibrium first, for Premium/Discount Array sequence testing") — adjacent to but not required by either the taxonomy file or the True Week Open edits above; thin enough on its own that it was left unadded rather than given a separate file touch. Logged here, not lost.
- **p.567's Higher-High/Lower-Low session-divergence note** (Pattern #2, Wednesday Low/High of Week) — unelaborated in the source beyond the bare instruction to check for it; kept as a one-clause mention inside the new taxonomy file's Pattern #2 row rather than expanded into its own criterion.

**Not paraphrased.** p.571's "8 เมตร" fragment (inside the Wednesday Weekly Reversal pattern's description) was unrecoverable OCR — dropped rather than guessed at, same precedent as this run's other rotated-page exclusions.

**Logged, not escalated.** p.565's "Open True Day ของวันพุธ" usage is a **seventh** Open True Day sighting, again drawn comparatively against a level — further firms, without upgrading, this run's standing "likely a misread of the CBDR chapter's usage" reading.

Updated `weekly-profile-patterns.md` (new), `weekly-quarters.md` (Thursday closing-time caution, Friday elaboration + TGIF reconciliation, cross-link, Source ID, Citations, Year Refined 2023→2026), `weekly-bias.md` (Pattern #1 cross-link, Source ID, Citations, Year Refined 2022→2026), `true-week-open.md` (two Wednesday checkpoints, Source ID, Citations, Year Refined 2023→2026), `tgif-weekly-po3.md` (reconciliation note in Common Mistakes). `SOURCES.md` (new entry), `TIMELINE.md` (2026 Year-Introduced/Refined bullet), `INDEX.md` (one new entry), `GLOSSARY.md` (Seek & Destroy entry). Ran `bin/lint-concepts.py` before presenting for review.

## [2026-08-15] ingest | Thai Community chapter — Trade Setup 2022 & Unicorn

Ingested "ICT Trade Setup.pdf" (23 pages: 1 unnumbered cover + pp.573–594). New Source ID `THAI-COMMUNITY-2026-TRADE-SETUP` — a twenty-first ingested chapter. **Not contiguous** with the immediately-prior ingest (`THAI-COMMUNITY-2026-DAILY-WEEKLY-PROFILE`, ending p.571) — p.572 is unaccounted-for, joining p.369, pp.428–435, and p.558 on the book's gap list. Surveyed `ict-2022-model.md`, `unicorn-model.md`, `internal-range-liquidity.md`, `ict-fib-overview.md`, `order-block-vs-supply-demand.md`, `equilibrium-definition.md`, `r-multiple.md`, `inversion-fvg.md`, `balanced-price-range.md` before scoping. `advisor()` caught the structural inversion that mattered before any file was touched.

**The structural call, inverted from last ingest.** Last ingest's call was "one file, not six" (a new taxonomy consolidating six named patterns). This chapter's two headline topics — the ICT 2022 Model and the Unicorn Model — turned out to already have dedicated, ICT-original, `confidence: high` files in this wiki (`ict-2022-model.md` sourced `ICT-2022-MENTORSHIP-OVERVIEW`, `unicorn-model.md` sourced `ICT-2023-UNICORN`), so the call inverted: enrich the two existing files in place rather than write a new `ict-2022-model-variants.md`. The Model 1→2→3→4 progression the source presents is additive confluence layers on one model — exactly what a Formal Criteria section is for, not a reason to fragment.

**The one genuine new concept: Inducement (IDM).** The source is explicit about its own vocabulary boundary (p.587): *"ในการศึกษา ICT Concept ไม่มี Inducement นะครับแต่มันจะมาจากศาสตร์ SMC"* — no Inducement in ICT study, it comes from SMC — then supplies its own translation: Inducement = this wiki's existing internal SSL/BSL (`internal-range-liquidity.md`). Wrote `inducement.md` at `confidence: community-attributed` with a mandatory `## ICT vs Community` section, following the same Bull-Trap/Clean-Traffic/SRF idiom this run has used before for non-ICT-original terms — cross-linked to `internal-range-liquidity.md` in both directions, names kept separate rather than merged, same precedent as `mitigation-block.md`. GLOSSARY.md already carried an unlinked "IDM" stub from an earlier session; updated it to point at the new file rather than adding a duplicate entry.

**The killzone question, settled before writing.** `ict-2022-model.md` makes killzone-window-active a mandatory step, with its own Common Mistakes calling pre/post-killzone setups disqualifying. None of this chapter's four Model 1–4 requirement lists (pp.577, 579, 581, 583, 585) names a killzone criterion. `advisor()` flagged the discriminating check before promoting this to a documented conflict: is this a stated time-agnostic claim, or just an omission from a bullet list? Re-checked the surrounding prose — no statement either way, just absence. Meanwhile the same chapter's Unicorn Model section (p.588) *does* state a time restriction explicitly — killzone-only, London or NY — which shows the author is capable of stating timing constraints when he means them, making the Model 1–4 silence read even more like ordinary omission than suppressed disagreement. Logged as absence only, per this run's standing "bare absence isn't evidence" precedent (the same one that excluded a bare fib-label diagram two ingests back).

**Four genuine, small additions to `ict-2022-model.md`.** (1) The Model 1→4 breakdown itself, folded into Formal Criteria as four stacking variants rather than four files. (2) Model 2's Inducement-sweep step, cross-linked to the new file, plus its alternate iFVG/BRP entry pairing (p.581) cross-linked to `inversion-fvg.md`/`balanced-price-range.md` — both already-documented concepts, newly paired here. (3) Model 3's Fibonacci OTE 62–79% entry — read `ict-fib-overview.md` directly rather than assumed, confirmed the exact same 0.62/0.705/0.79 numbers already documented there (including the 70.5% marked on the source's own diagram, p.583) — pure recap, logged as confirmation not addition. (4) Model 4's "Box Setup" — a distinct shape (multi-touch consolidation, HTF sweep, breakout, retest-as-order-block) verified against `order-block-vs-supply-demand.md` (no exact prior statement of consolidation-becomes-OB, closest existing ground) and the box's own 50%-entry refinement tied to `equilibrium-definition.md`'s existing EQ-on-any-reference-range mechanic rather than treated as new.

**One addition to `unicorn-model.md`, one corroboration.** The explicit killzone-only constraint (p.588) — genuinely new, this file previously carried no time criterion. And independent corroboration of the file's existing criterion 2 (breaker∩FVG overlap) via the source's own near-identical wording — logged as confirmation, not a rewrite.

**R:R > 1:2, logged not merged.** All five requirement lists in this chapter (four 2022-Model variants plus Unicorn) carry "R:R > 1:2" as a stated hard gate — stricter phrasing than `r-multiple.md`'s existing "<1.5R typically deprioritized" language. Logged as the source's own threshold on `ict-2022-model.md`; `r-multiple.md` itself left untouched, same precedent as not retroactively adjusting an existing file's numbers off a single new citation.

**Explicitly excluded, checked before excluding.** pp.573–576's points-per-timeframe averages (M1 100–150, M5 100–200, M15 200–400, H1 200–500, stated in **จุด** — the same broker-point unit that forced a correction in `central-bank-dealing-range.md` two ingests back, flagged explicitly here in case a later chapter restates these), lot-size 0.1 profit tables, and the author's 13-years-still-can't-do-it anecdote — no mechanics, same exclusion class as the earlier motivational-essay exclusion. p.584's requirement list is numbered 6–10, a source typo continuing Model 2's own numbering rather than a genuine ten-step model — not propagated.

Updated `inducement.md` (new), `ict-2022-model.md` (Model 1–4 breakdown, IDM/iFVG/BRP cross-links, OTE confirmation, Box Setup addition, R:R note, killzone-absence log, Source ID, Citations, Year Refined 2022→2026), `unicorn-model.md` (killzone-only constraint, breaker∩FVG corroboration, Source ID, Citations, Year Refined 2023→2026), `internal-range-liquidity.md` (Inducement cross-link, Source ID, Citations). `SOURCES.md` (new entry), `TIMELINE.md` (2026 Year-Introduced/Refined bullet), `INDEX.md` (one new entry), `GLOSSARY.md` (linked the pre-existing unlinked "IDM" stub). Ran `bin/lint-concepts.py` before presenting for review.

## [2026-08-15] ingest | Thai Community chapter — Daily Bias & Swing Point

Ingested "ICT Daily Bias Swing Point.pdf" (7 pages: cover **numbered** p.428 — the exception to every other chapter in this run, worth flagging on its own — plus pp.429–434). New Source ID `THAI-COMMUNITY-2026-DAILY-BIAS-SWING-POINT` — a twenty-second ingested chapter. Surveyed `daily-bias.md`, `swing-high.md`, `swing-low.md`, `cisd.md`, `top-down-analysis.md` before scoping. `advisor()` caught two record-keeping traps and one structural read before any file was touched.

**The gap reconciliation — the headline finding.** This chapter's pages (428–434) sit inside the long-open pp.428–435 gap first flagged at the Session Killzone ingest (twelfth). Before writing "p.435 still unaccounted," `advisor()` flagged that the Session Killzone entry's *own* text already reads its unnumbered cover as "presumably p.435" — checked directly rather than assumed, and it holds: **this chapter plus that cover page fully closes the pp.428–435 gap.** The book's unaccounted-for-pages list drops from {p.369, pp.428–435, p.558, p.572} to **{p.369, p.558, p.572}** — the single largest gap in the whole run, closed. Framed the ingest order correctly too: this is an out-of-order **back-fill**, book-contiguous with `-DAILY-BIAS-HOW-TO-USE` (ending p.427) despite being ingested twenty-second — same shape as the earlier `-DAILY-BIAS-ORDER-FLOW` back-fill, reusing that entry's own phrasing rather than describing this as "streak broken."

**Retiring a stub with a trail, not silently.** `daily-bias.md` has carried, since the tenth ingest, "Method 3 — past Swing Points. Named by the source but not elaborated in this chapter; not documented further here." `advisor()` named this the single edit most likely to be done sloppily — the standing rule from this run's own lessons is that a provisional hedge gets retired with an explicit "this was correct when written, a later chapter supplied it" trail, not a quiet overwrite. Wrote it that way.

**Method 3 is one mechanic, not three unrelated rules.** p.433 — the chapter's most legible page — gives a ranked anchor list: Equal High/Low → Previous Month High/Low → Previous Week High/Low → Previous Day High/Low → Previous Killzone High/Low. `advisor()` flagged the discriminating read before drafting: since Previous Day High/Low sits inside this list, Method 2's close-vs-PDH/PDL rule is the "Previous Day" tier of Method 3's ranked set, not a separate method — Method 3 generalizes the same close-through-the-level bias logic across five anchor scales, plus adds a **validity filter** (a 3-candle reversal cluster — Evening/Morning Star, Three Inside Down/Up — must mark the swing before it counts as a genuine anchor). Checked `top-down-analysis.md`'s existing Major/Medium/Minor Liquidity TF-tiers before treating the ranked list as new content: confirmed it's a different axis (TF-bucketing of generic liquidity swings vs. a specific anchor-priority order for one bias method) — not the same content restated.

**A repeated technique, not independent corroboration.** The 3-candle validity filter is the identical cluster list `cisd.md` already documents from a *different* chapter by the same author, used there to locate a CISD candle rather than validate a Daily Bias anchor. Same author reusing his own technique for a second purpose — logged as such, not counted as a second, independent source confirming the technique, per this run's own hedge-retraction-and-independence lesson. `swing-high.md`/`swing-low.md` gained the filter as a community-attributed alternative to their existing ICT 3-bar definitions, cross-linked to `cisd.md`; `cisd.md` itself gained only a one-line back-link, no new Source ID — the cluster list contributes no new claim to that file.

**The Period Separator table, checked rather than guessed.** p.433's MT4/MT5 charting table (Weekly chart → count-back unit "1 ปี," Daily → "1 เดือน," H4 → "1 สัปดาห์," H1-M15 → "1 วัน") read as nonsensical taken literally (a Weekly candle is obviously not one year). `advisor()` flagged this as the pp.369–400 mis-speculation pattern recurring — caught before writing it up as a guess. The coherent reading: MT4/MT5's **Period Separator** charting feature, set one level coarser than the working timeframe, so each separator-bounded block is where that tier's high/low is read from. Verified this reading makes all four rows internally consistent (each nests exactly one level coarser than the last) before citing it as a charting-convenience note, rather than accepting it on a single-row reading the way the pp.369–400 mistake did.

**Explicitly excluded, checked before excluding.** p.434's closing essay (the author's own "50 หน้า"/"80 หน้า" page-count remarks, general encouragement) — substantially garbled OCR, no mechanics beyond confirming the pre-existing three-method taxonomy (corroborating this chapter as Method 3) and a self-deferral to Session + Killzone as the next compounding topic — which resolves **backward**, not forward: Session and Killzone are both already thoroughly documented (`asia-session.md`/`london-session.md`/`ny-am-session.md`, `killzone-overview.md`) from earlier ingests in this run.

Updated `daily-bias.md` (Method 3 elaboration replacing the stub, Source ID, Citations), `swing-high.md`/`swing-low.md` (candle-cluster confirmation, `cisd.md` cross-link, Source ID, Citations, Year Refined 2017→2026), `cisd.md` (one-line back-link only). `SOURCES.md` (new entry with the gap-list correction spelled out), `TIMELINE.md` (2026 Year-Refined bullet). No `INDEX.md`/`GLOSSARY.md` changes, no new file. Ran `bin/lint-concepts.py` before presenting for review.
